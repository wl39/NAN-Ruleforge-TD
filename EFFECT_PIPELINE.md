# Ruleforge TD 효과 실행 파이프라인

## 1. 핵심 원칙

모든 효과는 다음 경로를 따른다.

```text
Trigger
  → Subject 선택
  → EffectExecutionContext 생성
  → ProgramSnapshot의 카드 실행
  → 등록된 IEffectExecutor가 시뮬레이션 파사드 호출
  → EffectExecutionOutcome으로 continuation 전달
  → GameEvent 예약
  → EventQueue 처리
```

파생 효과를 현재 호출 스택에서 즉시 재귀 실행하지 않는다. 분열, 폭발, 사망, 전염, 반복은 모두 새 `GameEvent`를 큐에 넣는다. 이 원칙은 실행 순서의 결정성, 브라우저 정지 방지, 연쇄 진단을 위한 필수 계약이다.

## 2. 이벤트 계약

`GameEvent`는 최소한 다음 정보를 가진다.

| 필드 | 의미 |
| --- | --- |
| `EventId` | 런 안에서 단조 증가하는 고유 ID |
| `RootChainId` | 최초 행동부터 이어진 연쇄 ID |
| `ParentEventId` | 이 이벤트를 만든 직접 부모, 없으면 0 |
| `ActivationId` | 한 타워/카드 프로그램 발동의 고유 ID |
| `EnqueueSequence` | 큐 등록 순서를 보존하는 단조 증가 값 |
| `EventType` | 이동, 적중, 카드 실행, 피해, 사망, 보상 등 |
| `EventPhase` | 동일 틱 내 처리 단계 |
| `SourceTowerId` | 최초 또는 현재 원본 타워 |
| `SourceCardId` | 직접 원인이 된 카드, 카드가 아니면 0 |
| `SourceEntityId` | 직접 원인이 된 적/탄환 |
| `SubjectEntityId` | 효과를 받는 대상 |
| `SubjectType` | `Projectile` 또는 `Enemy` |
| `Depth` | RootChain 안의 부모-자식 깊이 |
| `Generation` | 생성/복제/부활 세대 |
| `Tags` | 피해, 상태, 생성, 경제 등 규칙 태그 |
| `RewardOrigin` | 골드 사건의 출처 |
| `SimulationTick` | 처리 예정 틱 |

이벤트 정렬 키는 다음 순서다.

```text
SimulationTick
→ EventPhase
→ EnqueueSequence
```

`EnqueueSequence`는 같은 시점의 인과 순서를 보존한다. 해시 테이블의 열거 순서, Unity 충돌 콜백 순서, 프레임률을 정렬 기준으로 사용하지 않는다.

## 3. 틱과 이벤트 단계

| 단계 | 처리 내용 |
| --- | --- |
| `Command` | 이벤트 정렬상 예약된 논리 단계. 공개 명령은 `Submit` 호출 즉시 처리 |
| `Scheduled` | 지연 실행 사건 등록 |
| `Status` | 상태 틱과 상태 만료 사건 처리 |
| `Movement` | 적 이동, 경로 도착, 강제 이동 |
| `SpatialIndex` | 현재 논리 위치로 공간 인덱스 재구축 |
| `Tower` | 타워 쿨다운, 조건 트리거, 카드 프로그램 시작 |
| `Projectile` | 탄환 이동, 타깃 보정, 충돌 및 탄환 바인딩 |
| `Damage` | 피해 수정자와 체력 변경 |
| `Death` | 사망 판정, 부활, 사망 시 트리거 |
| `Reward` | 보상 원장 예약분 지급 |
| `Wave` | 스폰/종료/승패 판정 |
| `Presentation` | 규칙 처리가 끝난 의미 사건을 표현 버퍼에 기록 |

같은 대상에게 같은 단계의 사건이 여러 개면 등록 순서로 처리한다. 한 이벤트가 더 이른 단계의 새 이벤트를 만들더라도 현재 단계로 되감지 않는다. 새 이벤트는 현재 단계 이후의 적합한 단계 또는 다음 틱에 예약한다.

## 4. 프로그램 스냅샷과 실행 프레임

웨이브 시작 시 타워별 장착 카드 ID와 인스턴스 ID 배열을 복사해 고정한다. 문서에서 이 웨이브 복사본을 `ProgramSnapshot`이라 부른다. 기본 실행은 왼쪽에서 오른쪽이며 역순 실행 카드가 있는 문맥은 패스 시작 전에 오른쪽에서 왼쪽으로 한 번 확정한다.

현재 `ProgramFrame`은 다음 상태를 가진다.

- Subject 타입과 ID
- 타워 ID와 현재 카드 인덱스
- RootChain, Activation, 부모 Event, 깊이
- 분열이 미리 확보한 continuation 이벤트 수
- 순회 방향, 패스 위력, 반복 번호와 재진입 억제 플래그

각 카드는 현재 SubjectType에 맞는 프로그램만 사용한다. 양쪽 프로그램 중 하나라도 비어 있으면 콘텐츠 컴파일 실패다.

방향·위력·반복 번호·플래그는 `ProgramExecutionSpec`에 모아 모든 continuation이 그대로 이어받는다. 재귀와 신화 반복 횟수는 `ChainBudget`이 이벤트·카드 실행 예산과 같은 원자 예약에서 소비한다.

## 5. EffectExecutionContext와 실행 결과

`EffectExecutionContext`는 실행자가 읽을 수 있는 불변 입력이다.

- Subject 타입/ID, 타워 ID, 카드 ID와 카드 인스턴스 ID
- 직접 원인 개체 ID
- RootChain, Activation, 부모 Event, 깊이
- 남은 카드 수와 이미 예약된 continuation 수
- 현재 카드 인덱스, 순회 방향, 패스 위력, 반복 번호와 실행 플래그

`IEffectExecutor`는 구체 `GameSimulation` 전체가 아니라 효과 실행 전용 포트를 호출한다. 이 포트가 수치 변경, 상태/바인딩 적용, 개체 생성, 피해·이동·보상 이벤트와 예산 검증을 담당하므로 executor가 임의 컬렉션이나 Unity 객체를 만지지 않는다. 순수 `ContentCompiler`는 `IEffectOperationValidator`만 받고, `EffectContentCompiler`가 기본 descriptor registry를 조립하므로 콘텐츠 계층이 실행 계층을 역참조하지 않는다.

`EffectOperation`의 등록 단위는 다음 책임을 함께 가진 descriptor다.

- descriptor를 소유하는 효과 module ID
- 허용 실행 문맥(`Projectile`, `Enemy`, `Both`)
- 해당 operation의 executor
- JSON 매개변수 validator
- 등록 여부와 중복 등록 검증

기본 registry는 Core → Common → Uncommon → Rare → Legendary → Mythic의 명시적 순서로 조립된다. 같은 operation을 두 모듈이 등록하면 즉시 실패하며, 조립이 끝나면 enum 전체가 정확히 한 번 등록됐는지 확인한 뒤 동결한다. 콘텐츠 컴파일러와 `GameSimulation`은 이 단일 기본 registry를 공유한다.

콘텐츠 컴파일러는 별도의 거대 operation switch를 유지하지 않고 같은 descriptor registry에 검증을 위임한다. enum 값은 있지만 descriptor가 없거나, executor와 validator 중 하나가 빠진 operation, 또는 Projectile 전용 operation이 Enemy 해석에 들어간 경우는 콘텐츠 로딩 전에 실패한다. executor 진입점도 실행 문맥을 다시 확인해 컴파일된 콘텐츠가 아닌 잘못된 호출이 조용히 no-op이 되지 않게 한다.

현재 executor의 반환값은 `EffectExecutionOutcome`이며, 일반 계속 또는 최대 세 갈래로 파생된 원본/추가 Subject의 continuation 예약 정보를 프로그램 실행기에 전달한다. 원자성이 필요한 변경은 효과 파사드 안에서 예산과 큐 슬롯을 사전 예약한 뒤 적용한다.

## 6. 카드 커서와 생성 continuation

기본 규칙은 “생성 카드를 실행한 뒤의 카드만 이어서 실행”이다.

예를 들어 `[분열, 화상, 폭발]`에서 `분열`을 실행하면 다음과 같이 처리한다.

1. 원본은 첫 번째 가지로 유지한다.
2. 새 개체를 추가 가지로 만든다.
3. 모든 가지의 다음 커서는 `화상`이다.
4. 원본 가지는 분열 이전에 이미 얻은 상태와 발동 바인딩을 유지한다.
5. 새 가지는 분열 시점의 기본 물리 수치와 출처만 복사하고, 이전 카드가 만든 상태 및 발동 바인딩은 복사하지 않는다.

따라서 다음 두 프로그램은 달라야 한다.

- `[분열, 화상, 폭발]`: 두 가지 모두 이후 화상과 폭발을 획득한다.
- `[화상, 분열, 폭발]`: 원본 가지만 이전 화상을 유지하며, 두 가지 모두 이후 폭발을 획득한다.

적 분열도 같은 규칙을 사용한다. 단, 체력과 보상 할당은 `CARD_RULES.md`, `REWARD_RULES.md`의 적 분열 규칙을 따른다.

기본 상속 범위는 다음과 같다.

| 항목 | 원본 가지 | 새 가지 |
| --- | --- | --- |
| 엔티티 ID | 유지 | 새 ID |
| RootChain/원본 타워 | 유지 | 유지 |
| 분열 시점 물리 수치 | 유지 | 복사 후 카드 배율 적용 |
| 이전 카드 상태/바인딩 | 유지 | 미상속 |
| 다음 프로그램 커서 | 다음 카드 | 다음 카드 |
| 적 가계/보상 원장 | 유지 | 공유 |
| 이미 적중한 대상 목록 | 유지 | 비움 |

`과잉 복제`만 명시된 이전 카드 상태/바인딩을 새 가지에 상속할 수 있다. `재귀`, `마지막 명령`, `우로보로스`는 새 프로그램 패스를 예약할 수 있지만 생성 continuation 자체를 처음으로 되돌리지는 않는다.

## 7. 발동 바인딩

카드가 “적중 시”, “소멸 시”, “사망 시”, “이동 시” 효과를 말할 때 즉시 효과 대신 해당 Subject에 결정적 바인딩을 추가한다.

- 바인딩 키: 카드, 원본 타워, Activation, TriggerType
- 기본 수명: Subject가 제거될 때까지 또는 명시된 소비 횟수까지
- 동일 Activation에서 같은 일회성 바인딩은 중복 등록하지 않는다.
- 새 가지는 분열 이전 바인딩을 기본적으로 상속하지 않는다.
- 바인딩 발동도 새 이벤트를 예약하며 즉시 재귀하지 않는다.

Phase 1 예시는 다음과 같다.

- 탄환 화상: `OnHit`마다 적에게 화상을 적용하고 경로에 불길 예약
- 탄환 폭발: `FirstHitOrDespawn` 최초 1회에 폭발
- 폭발 범위: 원과 적의 데이터 기반 세로 캡슐 히트박스가 겹치면 대상에 포함하며, 경계 접촉도 적중으로 처리
- 탄환 밀치기: `OnHit`마다 진행 방향 강제 이동
- 탄환 표식/기절: `FirstHit` 1회
- 적 폭발: `OnDeath` 1회

## 8. 문법 카드

문법 카드는 수치 효과와 분리된 프로그램 제어 executor를 사용하며 현재 `EffectOperation`과 `EffectRegistry`에 등록되어 있다.

- `역순 실행`: 발동 시작 전에 실행 방향을 반대로 확정한다. 같은 프로그램 안에서 중첩 토글하지 않는다.
- `재귀`: 정상 패스 종료 후 첫 번째 실행 가능한 카드를 한 번 예약한다. RootChain당 한 번만 소비된다.
- `이중 해석`: 현재 실행 방향 기준의 다음 비문법 카드를 소비 대상으로 삼는다. 탄환 문맥은 해당 카드의 탄환 해석과 적중 대상의 적 해석을 함께 예약한다. 적 문맥은 적 해석과 같은 카드 탄환 해석을 가진 유령 탄환 생성을 예약한다.
- `마지막 명령`: Subject 소멸/사망 시 현재 스냅샷을 역방향으로 한 번 예약한다.
- `우로보로스`: 전체 패스를 감쇠시켜 반복하되 데이터의 최대 반복 횟수와 체인 예산을 동시에 적용한다.

반복 패스는 동일 RootChain의 토큰 집합을 공유한다. 이미 소비된 `재귀`, `이중 해석`, 부활, 복제 금지 토큰을 새 패스가 초기화하지 않는다.

## 9. 고티어 생명주기 순서

탄환의 한 소멸 시도는 다음 순서로 합성한다.

1. 일반 `OnFirstHitOrExpire` 바인딩을 확정한다.
2. `환원`이 성공하면 같은 탄환의 소멸을 취소한다.
3. `불사조 코어`가 성공하면 강화된 새 탄환으로 교체한다.
4. `환생`이 성공하면 유령 탄환을 만들고 원본을 종료한다.
5. 위 대체가 모두 실패한 소멸에서 `특이점` 저장 효과를 방출한다.
6. `마지막 명령`의 역순 패스를 예약한다. 이 패스가 끝난 뒤 같은 탄환의 최종 제거를 다시 예약한다.
7. 더 실행할 최종 효과가 없을 때 `Alive = false`로 확정한다.

환생·불사조처럼 새 `EntityId`를 만드는 대체 수명주기는 미사용 마지막 명령과 아직 방출되지 않은 특이점 payload를 새 개체로 이전하고 원본 권한을 제거한다. 따라서 원본과 대체체가 같은 최종 효과를 이중 발동하지 않는다.

적 사망은 `환생` → 신화 프록시 정리/`불사조 코어` → 희귀 기생·연쇄 최종 전이 → 전설 사망 효과 → 신화 특이점 최종 효과 → 일반 사망 바인딩·타워 트리거·보상 순서다. 시간 잔상과 거울 환영은 공격 가능한 프록시지만 본진 누출, 처치 보상, 웨이브 진행도와 일반 사망 타워 트리거를 만들지 않는다.

## 10. 연쇄 예산

초기 기본 한도는 다음과 같다.

| 예산 | 기본값 |
| --- | ---: |
| 한 RootChain 최대 깊이 | 8 |
| 한 RootChain 처리 이벤트 | 256 |
| 한 RootChain 탄환 생성 | 64 |
| 한 적 가계 분열 횟수 힌트 | 255 |
| 한 적 가계 누적 개체 비상 상한 | 256 |
| 한 탄환 도탄 | 8 |
| 한 탄환 관통 | 12 |
| 한 탄환 수명 | 450틱(15초) |
| 이벤트 큐 최대 대기 수 | 16,384 |
| 한 RootChain 카드 실행 | 32 |
| 한 RootChain 재귀 발동 | 1 |
| 한 RootChain 신화 반복 | 3 |
| 한 틱 전체 이벤트 | 4,096 |
| 동시 활성 불길/독안개 | 2,048 |
| 진단 원형 버퍼 | 256 |

예산은 효과를 일부 적용한 뒤 확인하지 않는다. 필요한 개체 수와 이벤트 수, 카드 실행 수, 큐 슬롯을 먼저 계산하고 한 번에 예약한다. 예약에 실패하면 해당 복합 효과 전체를 거절한다.

원자 예약이 적용되는 대표 복합 효과는 다음과 같다.

- 분열: 새 개체 생성 예산과 원본/자식의 남은 카드 continuation
- 폭발: 정렬된 유효 대상 전체의 피해 이벤트 배치
- 밀치기 충돌: 부딪힌 두 적의 피해 이벤트 쌍과 그 뒤의 강제 이동
- 프로그램 패스: 재귀·과부하·우로보로스가 실행할 전체 카드 수와 반복 토큰
- 마지막 명령: 선택된 모든 적의 역순 프로그램 패스 전체

불길과 독안개는 이벤트 예산뿐 아니라 `MaxActiveHazards = 2,048`을 확인한다. 상한을 넘으면 새 영역만 거절하고 `ActiveHazardLimitReached` 진단을 남긴다.

거절은 해당 원자 단위의 상태를 바꾸지 않고 진단 원형 버퍼와 `SafetyLimitReached` 표현 이벤트를 남긴다.

- `ChainDepthLimitReached`
- `ChainEventBudgetExceeded`
- `ProjectileSpawnBudgetExceeded`
- `EnemyLineageLimitReached`
- `CardTriggerLimitReached`
- `ProjectileLifetimeLimitReached`
- `TickEventBudgetExceeded`
- `EventQueueCapacityExceeded`
- `ActiveHazardLimitReached`
- `RecursionLimitReached`
- `MythicRepeatLimitReached`

한 틱 예산이 가득 찬 경우 규칙적으로 다음 틱으로 넘길 수 있다고 명시된 예약 이벤트만 넘긴다. 충돌, 피해, 사망처럼 현재 틱 원자성이 필요한 이벤트는 거절하고 진단한다.

Unity 표현 계층은 위 결정적 시뮬레이션 예산과 별도로 의미 기반 카드 VFX를
프레임당 최대 16개만 메시로 만든다. 초과 VFX는 누적 드롭 수만 기록하며 피해,
상태, 카드 실행과 리플레이 해시에는 영향을 주지 않는다. 위치 캐시는 의미 사건이
있는 프레임에만 한 번 생성하고, 같은 프레임에 분열·복제로 새로 생긴 개체는 최신
스냅샷에서 ID로 보완 조회한다.

## 11. 피해 파이프라인

피해는 다음 순서로 계산한다. Phase 1의 각 basis-point 곱셈과 방어력 나눗셈은 해당 단계에서 0 방향으로 정수 절삭한다. 이 절삭 위치도 결정성 계약이므로 플랫폼별 부동소수점으로 합치거나 순서를 바꾸지 않는다.

1. 카드/탄환의 원 피해
2. 공격자와 프로그램 위력 보정
3. 치명타
4. 표식, 단일 대상 취약, 범위 피해 취약
5. 속성 저항
6. 방어력과 관통/천공
7. 최소 피해 1 milliHP 보정
8. 보호막
9. 체력

지속 피해는 각 상태가 지정한 방어력 규칙을 사용한다. 중독은 방어력 일부를 무시하고, 속성 저항은 여전히 적용한다.

체력이 0 이하가 되면 즉시 여러 번 제거하지 않고 `PendingDeath`로 표시한다. 같은 틱에 들어온 선행 순서 피해를 모두 기록한 뒤 `Death` 단계에서 부활을 먼저 판정한다. 부활하지 않으면 사망을 한 번만 확정하고 사망 바인딩, 타워 트리거, 보상 순으로 사건을 예약한다.

## 12. 상태 해시와 재현성

상태 해시는 다음 값을 콘텐츠 정수 ID와 `EntityId` 순으로 직렬화해 계산한다.

- 콘텐츠 버전과 `ContentHash`, `RunConfig.DefinitionHash`
- 런 상태, 틱, 웨이브, 본진 체력, 골드
- 세 난수 스트림 상태
- 모든 타워, 적, 탄환, 상태이상, 바인딩
- 이벤트 큐와 연쇄 예산 사용량
- 가계 보상 원장과 웨이브 기여도
- 소유 카드, 장착 상태, 드래프트 제안

프레젠테이션 이벤트 읽기 위치, 자연어 로그, Unity 인스턴스 ID, 메모리 주소는 제외한다. 같은 콘텐츠 지문, 런 설정 지문, 시드, `Submit`/`Step` 호출 로그라면 매 틱 해시가 같아야 한다. `Submit`은 별도 명령 큐에 지연하지 않고 호출 즉시 검증·적용되므로 호출 순서 자체가 리플레이 계약이다.
