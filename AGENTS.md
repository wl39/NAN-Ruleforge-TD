# RULEFORGE TD
# Unity WebGL 기반 카드 조립형 타워 디펜스 마스터 개발 프롬프트

---

# 1. 역할

너는 다음 역할을 동시에 수행한다.

- 게임 디렉터
- 시스템 게임 디자이너
- 시니어 Unity 개발자
- 시니어 C# 개발자
- WebGL 최적화 엔지니어
- UI/UX 설계자
- 테스트 및 빌드 자동화 담당자

목표는 카드로 타워의 행동 규칙을 조립하는 로그라이트 타워 디펜스 게임을 개발하는 것이다.

단순히 화면이 동작하는 데모가 아니라, 새로운 카드와 타워를 지속적으로 추가할 수 있는 확장 가능한 구조로 개발한다.

작업 도중 사소한 의사결정을 위해 반복적으로 질문하지 말고 합리적인 기본값을 선택한다. 선택한 기본값은 코드, 설정 파일 또는 문서에 기록한다.

한 번에 전체 게임을 구현하지 말고, 각 개발 단계를 실행 가능한 상태로 완성한다.

---

# 2. 프로젝트 기본 정보

## 작업명

Ruleforge TD

게임명은 임시 명칭이며, 코드에 직접 하드코딩하지 않는다.

```csharp
public static class GameMetadata
{
    public const string DefaultGameTitle = "Ruleforge TD";
}
```

실제 표시 문자열은 별도의 로컬라이제이션 또는 텍스트 데이터에서 관리한다.

## 장르

* 로그라이트
* 타워 디펜스
* 카드 드래프트
* 자동 전투
* 빌드 조합
* 범용 중세 판타지
* 마법 및 기계 장치 테마

## 플랫폼

* 1차 플랫폼: 웹 브라우저
* 빌드 타깃: Unity WebGL
* 2차 확장 가능 플랫폼: Windows, macOS, 모바일

PC 버전을 먼저 완성한 뒤 웹으로 포팅하지 않는다.

프로젝트 시작 시점부터 WebGL 빌드 타깃을 설치하고, 모든 주요 단계에서 실제 브라우저 빌드를 검증한다.

## 아트 방향

CraftPix의 탑다운 타워 디펜스 픽셀 아트 에셋을 활용한다.

전체적인 분위기는 다음과 같다.

* 중세 판타지
* 성채와 마법 탑
* 마법 수정과 룬
* 기계식 포탑
* 몬스터와 갑옷 적
* 마법탄, 화염, 독, 번개, 얼음 효과
* 탑다운 또는 약간 기울어진 픽셀 아트 시점

세계관은 특정 현실 문화에 속하지 않는 범용 판타지로 유지한다.

---

# 3. 게임 핵심 콘셉트

타워는 문장의 앞부분을 제공하고, 플레이어는 카드를 장착해 문장의 뒷부분을 완성한다.

예시:

[공격할 때]
[탄환이]
[2개로 분열하고]
[적을 관통하며]
[적중 시 폭발한다]

다른 타워에서는 동일한 카드가 다음처럼 작동한다.

[탄환이 적에게 적중할 때]
[그 적이]
[2개로 분열하고]
[관통 상태가 되며]
[사망 시 폭발한다]

같은 카드를 어느 타워에 장착하느냐에 따라 탄환 효과 또는 적 효과로 해석한다.

게임의 핵심 한 문장은 다음과 같다.

“타워의 조건과 대상을 선택하고, 카드로 전투 규칙을 조립하는 타워 디펜스.”

---

# 4. 핵심 재미

이 게임의 핵심은 단순한 공격력 상승이 아니다.

플레이어가 카드 사이의 상호작용을 발견하고, 예상하지 못한 연쇄작용을 만들어 내는 것이 핵심이다.

대표적인 빌드:

적 분열
→ 적 숫자 증가
→ 화상과 중독이 더 많은 적에게 퍼짐
→ 처치 횟수 증가
→ 처치 시 폭발
→ 폭발이 주변 적의 상태이상을 다시 발동
→ 화면 전체가 연쇄적으로 폭발

플레이어가 다음 감정을 느끼게 한다.

* 같은 카드가 타워에 따라 다르게 작동한다.
* 카드 순서를 바꾸니 결과가 달라진다.
* 불리해 보이던 효과가 특정 빌드의 핵심이 된다.
* 내가 직접 강력한 조합을 발견했다.
* 다음 판에는 다른 타워와 카드 조합을 시도하고 싶다.

---

# 5. 절대 변경하지 않을 핵심 설계 원칙

## 5.1 모든 카드는 이중 해석을 가진다

모든 행동 카드는 반드시 다음 두 효과를 모두 정의한다.

* 탄환에 적용되었을 때의 효과
* 적에게 적용되었을 때의 효과

어떤 카드도 한쪽 대상에서 “효과 없음”으로 처리하지 않는다.

효과가 자연스럽지 않다면 무리하게 구현하지 말고, 카드 자체의 표현을 다시 설계한다.

예시:

분열

탄환 해석:
탄환이 두 발로 나뉜다.

적 해석:
적이 두 개체로 나뉜다.

## 5.2 타워가 실행 문맥을 결정한다

각 타워는 다음 정보를 가진다.

* Trigger: 언제 카드가 실행되는가
* SubjectType: 탄환 또는 적 중 무엇에 적용되는가
* SubjectSelector: 어떤 탄환 또는 적을 선택하는가
* Slots: 카드 장착 슬롯 수
* ComputeCapacity: 장착 가능한 카드 연산 비용
* Passive: 타워 고유 능력

## 5.3 카드 순서가 결과를 변경한다

카드는 왼쪽에서 오른쪽으로 실행한다.

예시 A:

[분열] → [화상] → [폭발]

탄환이 먼저 분열한다.
생성된 탄환들이 화상과 폭발 효과를 이어서 실행한다.

예시 B:

[화상] → [분열] → [폭발]

원본 탄환이 화상 효과를 먼저 실행한 뒤 분열한다.

두 조합은 서로 다른 결과를 만들어야 한다.

## 5.4 사기 조합은 허용하지만 실제 무한 루프는 금지한다

후반에는 다음과 같은 상황을 허용한다.

* 수십 개의 탄환이 화면을 가득 채움
* 화상과 중독이 적 무리 전체에 전염
* 처치가 연쇄 폭발을 일으킴
* 도탄과 관통이 반복됨
* 탄환이 소멸한 뒤 다시 되살아남

그러나 다음은 금지한다.

* 브라우저 정지
* 적 무한 증식
* 탄환 무한 생성
* 골드 무한 생성
* 이벤트 무한 재귀
* 한 프레임에 이벤트가 과도하게 몰리는 현상
* 같은 조합이 실행할 때마다 다른 결과를 만드는 비결정성

---

# 6. 게임 진행 구조

한 번의 런은 약 15~25분을 목표로 한다.

## 기본 진행

1. 시작 타워를 선택한다.
2. 기본 카드 몇 장을 받는다.
3. 타워를 정해진 건설 지점에 배치한다.
4. 웨이브를 시작한다.
5. 웨이브 종료 후 카드 3장 중 1장을 선택한다.
6. 계획 단계에서 카드를 타워 사이에 이동한다.
7. 카드 순서를 변경한다.
8. 일정 웨이브마다 신규 타워 또는 타워 업그레이드를 선택한다.
9. 중간 보스와 최종 보스를 상대한다.
10. 승리 또는 패배 후 빌드 통계를 확인한다.

## MVP 기준

* 맵 1개
* 고정된 적 이동 경로 1개
* 건설 지점 8~12개
* 8웨이브
* 4웨이브 중간 보스
* 8웨이브 최종 보스
* 전투 속도 1배 및 2배
* 일시정지
* 웨이브 사이 카드 재배치
* 전투 중 카드 변경 금지
* 카드 드래그 앤 드롭
* 간단한 전투 로그

초기 버전에서는 자유 배치보다 고정된 건설 지점을 사용한다.

---

# 7. 카드 티어

티어는 단순한 수치적 강함이 아니라, 카드가 전투 규칙을 얼마나 크게 변경하는지를 의미한다.

| 티어 | 등급 | 역할                  | 기본 연산 비용   |
| -- | -- | ------------------- | ---------- |
| 1  | 일반 | 단순하고 범용적인 원자 효과     | 1          |
| 2  | 고급 | 상태이상, 범위, 제어, 조건 효과 | 2          |
| 3  | 희귀 | 복제, 반복, 전염, 이벤트 연결  | 3          |
| 4  | 전설 | 카드 실행 순서와 문법 변경     | 4          |
| 5  | 신화 | 런의 핵심 규칙을 변경        | 5 또는 슬롯 2칸 |

초기 등장 확률 참고값:

* 일반: 48%
* 고급: 30%
* 희귀: 15%
* 전설: 6%
* 신화: 1%

모든 확률과 수치는 설정 데이터에서 변경할 수 있어야 한다.

---

# 8. 1티어 일반 카드

## 8.1 분열

탄환:
탄환이 총 2발로 나뉜다. 각 탄환은 원래 피해의 65%를 가진다.

적:
적이 최대 체력과 현재 체력의 45%를 가진 2개체로 나뉜다. 보상과 웨이브 진행도는 두 개체가 나누어 가진다.

태그:
생성, 다중화

## 8.2 관통

탄환:
추가로 적 2명을 관통한다. 관통할 때마다 피해가 10% 감소한다.

적:
천공 상태가 된다. 해당 적을 맞힌 탄환은 적을 통과하고, 적의 방어력 일부를 무시한다.

태그:
관통, 방어력 감소

## 8.3 화상

탄환:
적중한 적에게 화상을 부여하고 이동 경로에 짧은 불길을 남긴다.

적:
일정 시간 지속 화염 피해를 받는다. 화상 중첩이 높으면 주변 적에게 불씨가 전염될 수 있다.

태그:
화염, 지속 피해

## 8.4 둔화

탄환:
탄환 속도가 감소하지만 수명, 크기, 충돌 범위가 증가한다.

적:
이동 속도가 감소한다. 둔화 최대치는 60%로 제한한다.

태그:
제어, 속도

## 8.5 도탄

탄환:
적중 후 아직 맞히지 않은 가까운 적에게 튕긴다.

적:
밀치기 또는 에어본을 당할 때 주변 적을 향해 튕겨 나가고 충돌 피해를 준다.

태그:
연쇄, 이동

## 8.6 밀치기

탄환:
적중한 적을 탄환 진행 방향으로 밀어낸다.

적:
즉시 경로 뒤쪽으로 밀려난다. 다른 적과 충돌하면 양쪽이 피해를 받는다.

태그:
제어, 이동

## 8.7 표식

탄환:
처음 적중한 적에게 표식을 남긴다.

적:
받는 피해가 증가하며, 타워의 우선 공격 대상이 된다.

태그:
약화, 목표 지정

## 8.8 중독

탄환:
적중한 적에게 중독 중첩을 부여하고 짧은 독안개를 남긴다.

적:
중첩 가능한 지속 독 피해를 받는다. 중독 피해는 방어력 일부를 무시한다.

태그:
독, 지속 피해

## 8.9 거대화

탄환:
탄환 크기, 피해, 충돌 범위가 증가하지만 이동 속도가 감소한다.

적:
크기와 피격 범위가 증가하고 이동 속도가 감소한다. 범위 피해를 더 많이 받으며 처치 보상이 소폭 증가한다.

태그:
크기, 위험 보상

## 8.10 축소

탄환:
크기와 기본 피해가 감소하지만 탄속과 치명타 확률이 증가한다.

적:
최대 체력과 크기가 감소하지만 이동 속도가 증가한다. 단일 대상 공격에 더 많은 피해를 받는다.

태그:
크기, 속도

## 8.11 출혈

탄환:
적중하거나 관통할 때 출혈을 부여한다.

적:
이동하거나 밀려날 때 추가 출혈 피해를 받는다.

태그:
물리, 지속 피해

## 8.12 가속

탄환:
이동 속도가 증가하며, 날아간 거리에 비례해 피해가 증가한다.

적:
이동 속도와 처치 보상이 함께 증가한다.

태그:
속도, 위험 보상

## 8.13 추적

탄환:
가장 가까운 적 또는 표식이 있는 적을 추적한다.

적:
유도 가능한 탄환들이 해당 적을 우선 추적한다.

태그:
유도, 목표 지정

## 8.14 지연

탄환:
잠시 공중에 정지한 뒤 강화된 상태로 다시 움직인다.

적:
다음 이동 또는 특수 능력의 실행이 지연된다. 지속 피해 시간은 정상적으로 흐른다.

태그:
시간, 제어

---

# 9. 2티어 고급 카드

## 9.1 폭발

탄환:
첫 적중 또는 소멸 시 주변에 범위 피해를 준다.

적:
사망할 때 주변 적에게 범위 피해를 준다.

## 9.2 골드 획득

탄환:
새로운 적을 처음 적중할 때 소량의 골드를 생성한다. 탄환당, 타워당, 웨이브당 제한을 둔다.

적:
처치 보상이 증가한다. 분열체와 복제체는 부모의 보상 예산을 나누어 가진다.

## 9.3 저주

탄환:
적중한 적에게 저주를 부여한다.

적:
받는 상태이상 피해와 상태이상 지속시간이 증가한다. 사망할 때 저주 일부가 주변 적에게 옮겨간다.

## 9.4 속박

탄환:
처음 적중한 위치에 고정되어 일정 시간 함정처럼 파동을 발생시킨다.

적:
일정 시간 이동할 수 없다.

## 9.5 기절

탄환:
첫 적중 시 적을 짧게 기절시킨다.

적:
즉시 기절한다. 정예와 보스에게는 기절 대신 저항 게이지를 누적한다.

## 9.6 에어본

탄환:
포물선으로 날아가 목표 지점에 착탄하며 범위 피해를 준다.

적:
공중에 떠올라 이동하지 못한다. 착지할 때 주변 적에게 충돌 피해를 준다.

## 9.7 감전

탄환:
적중 후 전기가 가까운 적에게 연쇄된다.

적:
충전 중첩이 쌓이고, 최대 중첩에 도달하면 주변 적에게 전기를 방출한다.

## 9.8 빙결

탄환:
적중 시 냉기 중첩을 부여하고 소멸할 때 얼음 파편을 방출한다.

적:
냉기 중첩이 최대치에 도달하면 일정 시간 얼어붙는다.

## 9.9 잔상

탄환:
탄환이 지나간 경로를 잠시 후 약한 유령 탄환이 다시 따라간다.

적:
이동 경로에 잔상을 남긴다. 잔상이 받는 피해 일부가 원본 적에게 전달된다.

## 9.10 파동

탄환:
비행 중 주변에 주기적으로 약한 범위 파동을 방출한다.

적:
주기적으로 주변 적에게 자신이 가진 상태이상 일부를 퍼뜨린다.

## 9.11 자석

탄환:
가까운 아군 탄환을 끌어당겨 합체한다. 합체한 탄환은 피해와 카드 효과 일부를 가져온다.

적:
주변의 아군 탄환을 자신에게 끌어당긴다.

## 9.12 반사

탄환:
적중 후 타워 방향으로 되돌아왔다가 다시 적을 추적한다.

적:
해당 적에게 적중하거나 관통한 탄환이 가까운 다른 적에게 반사된다.

## 9.13 전염

탄환:
가까운 아군 탄환과 접촉할 때 자신이 가진 상태이상 카드 하나를 복사해 전달한다.

적:
일정 시간마다 자신이 가진 디버프 하나를 가까운 적에게 옮긴다.

## 9.14 봉인

탄환:
처음 적중한 적의 특수 능력을 일정 시간 막는다.

적:
보호막, 치유, 순간이동, 소환 등의 특수 능력을 사용할 수 없다.

## 9.15 부식

탄환:
적중할 때마다 적의 방어력을 감소시킨다.

적:
방어력과 최대 체력이 서서히 감소한다. 최대 체력 감소에는 하한선을 둔다.

## 9.16 회전

탄환:
처음 적중한 적 주위를 일정 시간 공전하며 여러 번 피해를 준다.

적:
현재 위치 주변을 회전하며 경로 진행이 느려지고 다른 적과 충돌한다.

## 9.17 흡혈

탄환:
입힌 피해 일부만큼 본진 체력 또는 타워의 과부하 자원을 회복한다.

적:
해당 적에게 입힌 피해 일부만큼 본진이 회복된다.

## 9.18 공포

탄환:
적중한 적이 일정 시간 경로 반대 방향으로 달아나게 한다.

적:
즉시 경로 뒤쪽으로 달아난다. 공포가 끝나면 잠시 이동 속도가 증가한다.

---

# 10. 3티어 희귀 카드

## 10.1 복제

탄환:
자신과 동일한 유령 탄환을 하나 생성한다. 복제 탄환은 다시 복제할 수 없다.

적:
현재 상태를 가진 유령 복제체를 생성한다. 복제체는 원본과 체력 일부를 공유하며 추가 보상을 주지 않는다.

## 10.2 희생

탄환:
자신을 소멸시키고 가까운 아군 탄환에 피해와 남은 카드 효과를 전달한다.

적:
현재 체력 일부를 잃고 그만큼 주변 적에게 피해를 나누어 준다.

## 10.3 환원

탄환:
소멸할 때 타워로 돌아와 한 번 다시 발사된다.

적:
몇 초 전의 경로 위치로 되돌아간다. 체력과 상태이상은 현재 상태를 유지한다.

## 10.4 역행

탄환:
마지막 적중 후 지나온 경로를 반대로 이동하며 다시 피해를 준다.

적:
일정 시간 경로를 반대 방향으로 이동한다.

## 10.5 공명

탄환:
같은 태그를 가진 탄환이 가까이 있으면 서로의 효과가 강화된다.

적:
같은 상태이상을 가진 적끼리 연결되어 상태이상 피해와 지속시간이 증가한다.

## 10.6 흡수

탄환:
가까운 아군 탄환을 흡수해 피해, 크기, 카드 효과 하나를 가져온다.

적:
체력이 낮은 주변 적을 흡수해 체력과 상태이상을 가져온다. 전체 보상량은 증가하지 않는다.

## 10.7 시간 정지

탄환:
공중에서 정지한 동안 파동과 지속 효과를 저장하고, 해제될 때 한꺼번에 방출한다.

적:
완전히 정지한다. 화상, 중독, 출혈 피해는 계속 적용된다.

## 10.8 변이

탄환:
장착 카드 하나가 같은 티어의 다른 카드 효과로 일시적으로 변한다.

적:
다른 적 형태로 변한다. 약점 하나와 강화 효과 하나를 얻는다.

## 10.9 처형

탄환:
체력이 낮은 적에게 큰 피해를 주고 즉시 소멸한다.

적:
처형 표식이 생기며 일정 체력 이하에서 즉시 사망한다.

## 10.10 기생

탄환:
적 내부에 박혀 일정 시간마다 남은 카드 효과를 반복한다.

적:
기생체가 붙어 주기적으로 피해를 받고, 사망 시 기생체가 가까운 적에게 이동한다.

## 10.11 환생

탄환:
소멸 후 한 번 유령 탄환으로 되살아난다.

적:
사망 후 약한 영혼 형태로 되돌아와 경로를 반대로 이동한다. 추가 보상은 없다.

## 10.12 연쇄

탄환:
모든 카드 실행이 끝나면 가까운 탄환 하나가 같은 카드 순서를 약화된 위력으로 실행한다.

적:
사망할 때 가까운 적에게 자신에게 적용된 카드 순서를 약화된 위력으로 전달한다.

---

# 11. 4티어 전설 카드

## 11.1 재귀

탄환:
마지막 카드 실행 후 첫 번째 카드를 한 번 더 실행한다.

적:
모든 적 대상 카드가 끝난 뒤 첫 번째 카드를 다시 실행한다.

한 연쇄에서 1회만 발동한다.

## 11.2 역순 실행

탄환:
장착 카드가 오른쪽에서 왼쪽 순서로 실행된다.

적:
적에게 적용되는 카드 순서가 반대로 바뀐다.

## 11.3 이중 해석

탄환:
다음 카드의 탄환 효과와 적 효과를 동시에 실행한다. 적 효과는 적중 대상에게 적용한다.

적:
다음 카드의 적 효과를 실행하고, 같은 카드의 탄환 효과를 가진 유령 탄환을 생성한다.

## 11.4 무한 궤도

탄환:
최대 적중 횟수에 도달할 때까지 소멸하지 않고 목표 주변을 순환한다.

적:
일정 횟수 피격될 때까지 짧은 경로 구간을 반복해서 걷는다.

실제 무한 반복은 허용하지 않는다.

## 11.5 과잉 복제

탄환:
분열 또는 복제된 탄환이 분열 이전 카드 일부도 물려받는다.

적:
분열 또는 복제된 적이 상태이상과 사망 효과 일부를 물려받는다.

## 11.6 금단의 거래

탄환:
적중할 때 골드를 소비하여 카드 순서를 한 번 더 실행한다. 골드가 부족하면 탄환이 소멸한다.

적:
살아 있는 동안 골드를 생성하지만 시간이 지날수록 크기, 체력, 이동 속도가 증가한다.

## 11.7 마지막 명령

탄환:
소멸할 때 장착 카드를 역순으로 한 번 더 실행한다.

적:
사망할 때 장착 카드를 역순으로 주변 적에게 적용한다.

## 11.8 운명 고정

탄환:
확률 효과가 무작위 대신 정해진 주기로 반드시 발동한다.

적:
다음 확률형 효과가 반드시 발동하지만 위력이 조금 감소한다.

## 11.9 과부하

탄환:
모든 카드를 즉시 한 번 더 실행한 뒤 폭발하며 소멸한다.

적:
모든 적 대상 카드를 한 번 더 실행하지만, 이후 일정 시간 이동 속도와 상태이상 저항을 얻는다.

---

# 12. 5티어 신화 카드

## 12.1 특이점

탄환:
주변 탄환을 끌어당겨 흡수하고, 소멸할 때 흡수한 탄환들의 카드 효과를 범위로 방출한다.

적:
주변 적을 끌어당기며 피해 일부를 공유한다. 사망할 때 큰 폭발을 일으킨다.

## 12.2 불사조 코어

탄환:
소멸 후 한 번 더 강한 형태로 부활한다. 부활 후에는 다시 부활할 수 없다.

적:
사망 후 낮은 체력으로 한 번 부활한다. 추가 보상은 없으며, 기존 상태이상은 취약 효과로 변환된다.

## 12.3 시간 균열

탄환:
현재 탄환의 과거 잔상과 미래 잔상을 각각 하나 생성한다.

적:
과거 위치와 예상 미래 위치에 시간 잔상을 생성한다. 잔상이 받는 피해 일부가 원본에게 전달된다.

## 12.4 거울 세계

탄환:
타워 반대편 방향에 동일한 카드 효과를 가진 거울 탄환을 생성한다.

적:
체력을 공유하는 환영 적을 만든다. 환영은 추가 보상과 웨이브 진행도를 주지 않는다.

## 12.5 우로보로스

탄환:
카드 순서가 끝나면 위력이 감소한 상태로 전체 순서를 다시 실행한다. 최대 반복 횟수를 제한한다.

적:
카드 순서가 끝나면 가까운 다른 적에게 전체 카드 순서를 전달하고 반복한다. 전달될 때마다 위력이 감소한다.

---

# 13. 카드 강화

카드 희귀도와 카드 강화 레벨을 분리한다.

각 카드는 기본적으로 1~3레벨을 가진다.

예시:

분열 I:
탄환 2개, 탄환당 피해 65%

분열 II:
탄환 2개, 탄환당 피해 75%

분열 III-A:
탄환 3개, 탄환당 피해 60%

분열 III-B:
탄환 2개, 탄환당 피해 100%

최종 강화에서는 단순 수치 증가보다 선택형 변화를 제공한다.

카드의 핵심 정체성은 유지하되 빌드 방향이 달라지게 한다.

---

# 14. 타워 시스템

## 14.1 발리스타 타워

문장:

[공격할 때]
[탄환이]
[카드 1]
[카드 2]
[카드 3]

대상:
PROJECTILE

콘셉트:
범용 기본 타워

슬롯:
3

연산력:
5

추천 카드:
분열, 관통, 화상, 추적, 도탄

---

## 14.2 캐논 바스티온

문장:

[세 번째 공격마다]
[대포탄이]
[카드 1]
[카드 2]
[카드 3]
[카드 4]

대상:
PROJECTILE

콘셉트:
느리지만 큰 범위 피해와 밀치기

슬롯:
4

연산력:
7

기본 패시브:
탄환 크기 증가, 약한 폭발, 높은 밀치기

추천 카드:
거대화, 폭발, 에어본, 밀치기, 특이점

---

## 14.3 리피터 아웃포스트

문장:

[같은 적을 세 번 공격하면]
[추가 탄환이]
[카드 1]
[카드 2]
[카드 3]

대상:
PROJECTILE

콘셉트:
빠른 공격과 적중 횟수 기반 빌드

슬롯:
3

연산력:
5

추천 카드:
표식, 중독, 감전, 골드 획득, 분열

---

## 14.4 비콘 스파이어

문장:

[적이 처음 사거리에 들어오면]
[신호탄이]
[카드 1]
[카드 2]
[카드 3]

대상:
PROJECTILE

콘셉트:
긴 사거리와 선제 공격

슬롯:
3

연산력:
5

추천 카드:
표식, 저주, 추적, 관통, 봉인

---

## 14.5 오비탈 코어

문장:

[탄환이 소멸할 때]
[궤도 탄환이]
[카드 1]
[카드 2]
[카드 3]

대상:
PROJECTILE

콘셉트:
소멸, 귀환, 공전, 유령 탄환

슬롯:
3

연산력:
6

추천 카드:
환원, 역행, 회전, 잔상, 환생

---

## 14.6 뮤테이션 오벨리스크

문장:

[적이 사거리에 들어오면]
[그 적이]
[카드 1]
[카드 2]
[카드 3]

대상:
ENEMY

콘셉트:
적을 직접 변형하는 타워

슬롯:
3

연산력:
5

추천 카드:
분열, 거대화, 저주, 표식, 변이

같은 적에 대한 재발동 대기시간을 둔다.

---

## 14.7 배리어 게이트

문장:

[적이 결계선을 넘으면]
[그 적이]
[카드 1]
[카드 2]
[카드 3]

대상:
ENEMY

콘셉트:
경로 제어와 반복 통과 빌드

슬롯:
3

연산력:
5

추천 카드:
속박, 밀치기, 에어본, 기절, 역행

---

## 14.8 펄스 모놀리스

문장:

[일정 시간마다]
[범위 안의 모든 적이]
[카드 1]
[카드 2]

대상:
ENEMY

콘셉트:
넓은 범위에 상태이상 적용

슬롯:
2

연산력:
5

추천 카드:
둔화, 저주, 중독, 빙결, 전염

범위 타워이므로 슬롯 수를 적게 유지한다.

---

## 14.9 데스 엔진

문장:

[적이 사망하면]
[주변 적이]
[카드 1]
[카드 2]
[카드 3]

대상:
ENEMY

콘셉트:
처치 연쇄와 사망 효과

슬롯:
3

연산력:
6

추천 카드:
폭발, 화상, 중독, 분열, 연쇄

---

## 14.10 알케미 랩

문장:

[한 적에게 서로 다른 상태이상 3개가 쌓이면]
[그 적이]
[카드 1]
[카드 2]
[카드 3]

대상:
ENEMY

콘셉트:
상태이상 조합 완성 보상

슬롯:
3

연산력:
6

추천 카드:
시간 정지, 처형, 전염, 기생, 부식

같은 적에 대한 재발동 대기시간을 둔다.

---

## 14.11 익스큐션 게이트

문장:

[적의 체력이 25% 이하가 되면]
[그 적이]
[카드 1]
[카드 2]
[카드 3]

대상:
ENEMY

콘셉트:
처형과 마무리 빌드

슬롯:
3

연산력:
6

추천 카드:
처형, 골드 획득, 환생, 폭발, 마지막 명령

적 하나당 한 번만 발동한다.

---

## 14.12 워든 스파이어

문장:

[정예 또는 보스가 특수 능력을 사용하면]
[그 적이]
[카드 1]
[카드 2]

대상:
ENEMY

콘셉트:
정예와 보스 대응

슬롯:
2

연산력:
6

추천 카드:
봉인, 저주, 부식, 시간 정지, 기절

---

## 14.13 아케인 포지

문장:

[일정량의 골드를 획득하거나 소비하면]
[마법 탄환이]
[카드 1]
[카드 2]
[카드 3]
[카드 4]

대상:
PROJECTILE

콘셉트:
경제와 공격을 연결하는 타워

슬롯:
4

연산력:
7

추천 카드:
골드 획득, 분열, 금단의 거래, 과부하

이 타워 또는 카드가 생성한 골드는 동일한 경제 트리거를 다시 발생시키지 않는다.

---

## 14.14 헥스파이어 스파이어

문장:

[상태이상이 전염되거나 만료되면]
[마법 불꽃 탄환이]
[카드 1]
[카드 2]
[카드 3]

대상:
PROJECTILE

콘셉트:
상태이상의 전염과 만료를 공격 기회로 전환

슬롯:
3

연산력:
6

추천 카드:
화상, 저주, 전염, 잔상, 공명

---

## 14.15 솔라 코어

문장:

[화상 피해가 일정량 누적되면]
[태양 탄환이]
[카드 1]
[카드 2]
[카드 3]

대상:
PROJECTILE

콘셉트:
화상 빌드의 최종 공격 엔진

슬롯:
3

연산력:
7

추천 카드:
화상, 파동, 폭발, 과부하, 특이점

---

## 14.16 듀얼리티 코어

홀수 번째 발동:

[공격할 때]
[탄환이]
[카드 1]
[카드 2]
[카드 3]
[카드 4]

짝수 번째 발동:

[탄환이 적중할 때]
[그 적이]
[카드 1]
[카드 2]
[카드 3]
[카드 4]

대상:
PROJECTILE과 ENEMY를 번갈아 사용

콘셉트:
동일 카드의 두 해석을 모두 사용하는 고난도 타워

슬롯:
4

연산력:
7

이 타워는 게임을 대표하는 핵심 타워로 활용한다.

---

## 14.17 릴레이 아카이브

문장:

[인접 타워가 마지막 카드를 실행하면]
[동일한 대상이]
[카드 1]
[카드 2]

대상:
인접 타워의 SubjectType을 상속

콘셉트:
인접 타워의 실행 문맥을 복사하는 지원 타워

슬롯:
2

연산력:
6

발리스타 옆에서는 탄환 카드로, 뮤테이션 오벨리스크 옆에서는 적 카드로 해석한다.

---

## 14.18 소울 리액터

문장:

[탄환 또는 적이 소멸하면]
[그 존재의 잔재가]
[카드 1]
[카드 2]
[카드 3]

대상:
원본 존재의 타입에 따라 결정

콘셉트:
소멸과 사망을 새로운 자원으로 사용

슬롯:
3

연산력:
7

추천 카드:
환생, 환원, 마지막 명령, 불사조 코어, 우로보로스

---

# 15. 동일 카드 조합의 타워별 차이

카드 조합:

[분열] → [화상] → [폭발]

## 발리스타 타워

공격 시:

* 탄환이 2개로 분열
* 각 탄환이 화상을 부여
* 각 탄환이 적중 또는 소멸 시 폭발

## 뮤테이션 오벨리스크

적 진입 시:

* 적이 2개체로 분열
* 분열한 적들이 화상 상태가 됨
* 각 적이 사망할 때 폭발

## 데스 엔진

적 사망 시 주변 적들이:

* 분열
* 화상 상태가 됨
* 사망 시 폭발 속성을 얻음

같은 카드 3장을 사용해도 전혀 다른 전투 결과가 나와야 한다.

---

# 16. 카드 드래프트

웨이브 종료 후 카드 3장을 제시한다.

선택지는 다음 규칙으로 생성한다.

1. 현재 보유한 타워 중 하나에 효과적으로 사용할 수 있는 카드
2. 현재 빌드 태그와 시너지가 있는 카드
3. 무작위 카드

현재 빌드에 맞는 카드만 제공하지 않는다.

플레이어가 새로운 빌드로 전환할 가능성과 예상하지 못한 조합을 발견할 가능성을 유지한다.

주요 빌드 태그:

* 화상
* 중독
* 출혈
* 저주
* 제어
* 탄환 생성
* 적 분열
* 처치
* 골드
* 소멸
* 관통
* 도탄
* 복제
* 시간
* 크기
* 이동

---

# 17. 적 시스템

## 적 스프라이트 파일명 및 방향 선택 규칙

적 스프라이트 시트 파일명은 다음 규칙을 따른다.

```text
<Direction first letter>_<Behaviour>.png
```

현재 에셋의 방향 접두사는 다음과 같이 해석한다.

* `D`: Down, 화면 아래 방향
* `U`: Up, 화면 위 방향
* `S`: Side, 화면 좌우 방향

행동 이름은 `Walk`, `Attack`, `Death`, `Special` 등을 사용한다. `Walk2`, `Death2`처럼 숫자가 붙은 파일은 같은 행동의 별도 변형으로 취급한다.

적이 움직일 때 실제 이동 벡터에 맞는 방향 에셋을 선택한다.

* 수직 이동이 우세하고 아래로 이동하면 `D_<Behaviour>`를 사용한다.
* 수직 이동이 우세하고 위로 이동하면 `U_<Behaviour>`를 사용한다.
* 수평 이동이 우세하면 `S_<Behaviour>`를 사용한다.
* 동쪽과 서쪽 방향은 서로 좌우 반전 관계이므로 별도 파일을 요구하지 않는다. 제공된 `S` 에셋 하나를 사용하고 반대 방향은 `SpriteRenderer.flipX`로 표현한다.
* 수평 방향 파일은 두 방향 중 하나만 존재하는 것을 정상으로 간주하며, 누락된 반대 방향 파일을 찾거나 복제하지 않는다.
* 위/아래 방향 에셋에는 좌우 반전을 적용하지 않는다.

방향 판정과 애니메이션 선택은 렌더링 계층에서 처리하고, 이동 시뮬레이션은 특정 스프라이트 파일명에 의존하지 않는다.

## 현재 CraftPix 적 기본 체력

현재 테스트 및 프리팹에 사용하는 적의 기본 최대 체력은 다음과 같다.

* Slime: 10
* Bee: 5
* Dog: 8
* Goblin: 30

체력 수치는 프리팹의 `EnemyHealth` 데이터로 설정하고, 피해로 체력이 0이 되면 현재 방향에 맞는 `Death` 애니메이션을 재생한다. `Death2`가 제공된 적은 별도 사망 연출 변형으로 사용할 수 있다.

## 기본 적 종류

### Raider

평균적인 체력과 이동 속도.

### Runner

체력이 낮지만 이동 속도가 빠르다.

### Armored Knight

높은 방어력과 낮은 이동 속도.

### Swarm Creature

체력은 낮지만 다수로 등장한다.

### Fire Elemental

화상에 강하고 빙결에 약하다.

### Plague Creature

중독에 강하고 화상에 약하다.

### Support Mage

주변 적에게 보호막 또는 회복을 제공한다.

### Phase Walker

주기적으로 경로 일부를 순간이동한다.

### Elite Golem

높은 제어 저항과 강화 효과를 가진 정예 적.

### Boss

여러 단계의 특수 능력을 사용하는 대형 적.

## 보스 상태이상 규칙

보스가 기절, 속박, 에어본에 완전히 면역이 되게 하지 않는다.

강한 제어 효과는 보스 저항 게이지로 변환한다.

예시:

일반 적 기절:
1초 동안 행동 불가

보스 기절:
저항 게이지 30 증가

저항 게이지 100 도달:
보스 행동을 0.8초 방해
저항 게이지 초기화

반복해서 제어하면 일시적으로 저항 증가량이 낮아지도록 하지 말고, 보스의 저항력이 서서히 증가하도록 한다.

---

# 18. 상태이상 시스템

각 상태이상은 다음 정보를 가진다.

* id
* sourceEntityId
* sourceTowerId
* sourceCardId
* stacks
* intensity
* duration
* remainingDuration
* maxStacks
* refreshPolicy
* tickInterval
* tags
* inherited
* dispellable

## 기본 상태이상 규칙

### 화상

* 짧은 지속시간
* 빠른 피해 틱
* 중첩 가능
* 전염 빌드와 시너지

### 중독

* 긴 지속시간
* 느린 피해 틱
* 방어력 일부 무시
* 높은 중첩 시 효과 증가

### 출혈

* 이동, 밀치기, 에어본 시 추가 피해
* 움직임 제어 카드와 시너지

### 저주

* 다른 상태이상의 피해와 지속시간 증가
* 사망 시 전염 가능

### 표식

* 받는 피해 증가
* 타워 우선 공격 대상

### 둔화

* 이동 속도 감소
* 최대 둔화 60%

### 빙결

* 냉기 중첩이 최대치에 도달하면 발동
* 빙결 후 짧은 면역 시간

### 기절

* 짧고 강한 행동 방해

### 속박

* 이동만 제한
* 지속 피해와 피격은 정상 적용

### 에어본

* 공중에 떠 있는 동안에도 타워가 추적 가능
* 무적 상태로 처리하지 않음

### 봉인

* 적의 특수 능력 사용 제한

### 부식

* 방어력 감소
* 최대 체력 감소
* 감소 하한선 존재

---

# 19. 카드 실행 구조

모든 전투 효과는 다음 파이프라인을 따른다.

Trigger 발생
→ Subject 선택
→ EffectContext 생성
→ 장착 카드 배열을 순서대로 실행
→ EffectResult 생성
→ 새로운 GameEvent를 EventQueue에 등록
→ 다음 이벤트 처리

동일 호출 스택에서 재귀적으로 효과를 바로 실행하지 않는다.

새로운 효과는 반드시 EventQueue를 통해 처리한다.

## GameEvent 필수 정보

* EventId
* RootChainId
* ParentEventId
* EventType
* SourceTowerId
* SourceCardId
* SourceEntityId
* SubjectEntityId
* SubjectType
* Depth
* Generation
* Tags
* RewardOrigin
* SimulationTick

---

# 20. 생성된 개체의 카드 상속

기본 규칙:

새로 생성된 탄환 또는 적은 자신을 생성한 카드 이후에 남아 있는 카드만 이어서 실행한다.

예시:

[분열] → [화상] → [폭발]

분열 카드가 새 탄환을 생성하면 새 탄환은 다음을 실행한다.

[화상] → [폭발]

새 탄환이 다시 처음부터 분열을 실행하지 않는다.

적 분열은 카드 실행 순서와 별개로 분열 직전 적이 가진 모든 상태이상의
런타임 상태를 두 결과에 깊은 복제한다. 중첩, 강도, 남은 시간, 다음 틱,
피해 누적값과 출처를 복제하되 각 가지가 이후 독립적으로 변하도록 새 상태
인스턴스 ID를 발급한다. 사망 바인딩은 상태이상이 아니므로 자동 복제하지
않으며, 분열 이후 카드로 등록된 사망 효과만 두 가지가 이어서 실행한다.

다음 카드가 명시적으로 있을 때만 예외를 허용한다.

* 재귀
* 과잉 복제
* 우로보로스
* 마지막 명령

---

# 21. 무한 루프 방지

초기 안전 한도:

* 한 원본 행동의 최대 연쇄 깊이: 8
* 한 연쇄의 최대 이벤트 처리 수: 256
* 한 연쇄에서 생성 가능한 탄환: 64
* 적 분열 결과의 최대 체력과 현재 체력이 모두 1 이상일 때만 분열 허용
* 한 탄환의 최대 도탄 횟수: 8
* 한 탄환의 최대 관통 횟수: 12
* 한 탄환의 최대 생존 시간: 15초
* 재귀의 한 연쇄 최대 발동: 1
* 복제 탄환의 재복제 기본 금지
* 복제 적의 추가 보상 금지
* 신화 카드 반복 횟수 제한

안전 한도에 도달한 효과를 조용히 무시하지 않는다.

개발 모드 로그에 다음과 같은 원인을 기록한다.

* CHAIN_DEPTH_LIMIT_REACHED
* CHAIN_EVENT_BUDGET_EXCEEDED
* PROJECTILE_SPAWN_BUDGET_EXCEEDED
* CARD_TRIGGER_LIMIT_REACHED
* PROJECTILE_LIFETIME_LIMIT_REACHED

---

# 22. 골드와 보상 규칙

각 적은 rewardBudget을 가진다.

분열 또는 복제 시 전체 rewardBudget은 증가하지 않는다.

예시:

부모 적:
10골드

2개로 분열:
각각 최대 5골드

다시 분열:
가계 전체 합계는 여전히 최대 10골드

다음과 같은 위험형 카드만 보상 예산을 증가시킬 수 있다.

* 거대화
* 가속
* 골드 획득
* 금단의 거래

골드에는 반드시 생성 출처를 기록한다.

```csharp
public enum RewardOrigin
{
    EnemyDrop,
    CardBounty,
    TowerTrigger,
    ShopRefund,
    Debug
}
```

CardBounty와 TowerTrigger로 생성한 골드는 동일한 경제 타워 또는 골드 카드를 다시 발동시키지 않는다.

---

# 23. Unity 기술 스택

## 필수 기술

* Unity LTS
* C#
* Unity 2D
* WebGL Build Target
* uGUI 기반 런타임 UI
* NUnit 기반 EditMode 테스트
* PlayMode 테스트
* ScriptableObject
* JSON 설정 데이터
* Object Pool
* Sprite Atlas
* 고정 시뮬레이션 틱
* Seeded Random

MVP에서는 DOTS, ECS, 복잡한 Job System을 사용하지 않는다.

프로파일링 결과 실제로 필요하다고 판단된 경우에만 별도 단계에서 검토한다.

## WebGL 우선 원칙

* 모든 주요 기능은 WebGL 빌드에서 검증한다.
* 운영체제 파일 시스템에 직접 의존하지 않는다.
* 플랫폼 전용 네이티브 플러그인을 사용하지 않는다.
* 백그라운드 스레드에 의존하는 구조를 만들지 않는다.
* 데스크톱 에디터에서만 동작하는 API를 런타임 코드에 사용하지 않는다.
* 저장 시스템은 인터페이스로 추상화한다.
* 웹 브라우저 포커스를 잃으면 자동 일시정지할 수 있게 한다.

---

# 24. 시뮬레이션과 렌더링 분리

게임 규칙과 Unity 오브젝트 렌더링을 분리한다.

## 시뮬레이션

다음 시스템이 담당한다.

* 이동
* 공격
* 타깃 탐색
* 카드 효과
* 상태이상
* 이벤트 큐
* 피해 계산
* 골드
* 웨이브
* 연쇄 예산

## 렌더링

다음 시스템이 담당한다.

* 스프라이트 표시
* 애니메이션
* 파티클
* 데미지 텍스트
* 체력바
* 타워 사거리
* 카드 발동 아이콘

MonoBehaviour의 Update에 핵심 게임 규칙을 분산시키지 않는다.

가능한 한 중앙 시스템이 고정 틱으로 처리한다.

권장 시뮬레이션 속도:

* 초당 30틱

렌더링은 브라우저 프레임에 맞춰 위치를 보간한다.

같은 시드와 같은 입력이면 같은 전투 결과가 나오도록 한다.

---

# 25. 데이터 중심 카드 구조

카드별로 거대한 if/else 또는 switch 문을 만들지 않는다.

금지 예시:

```csharp
if (cardId == "split")
{
    // ...
}
else if (cardId == "burn")
{
    // ...
}
else if (cardId == "poison")
{
    // ...
}
```

EffectRegistry를 사용한다.

```csharp
public interface IEffectExecutor
{
    IReadOnlyList<EffectResult> Execute(
        in EffectContext context,
        EffectNode node
    );
}
```

```csharp
public sealed class EffectRegistry
{
    private readonly Dictionary<EffectOperation, IEffectExecutor> executors;

    public IEffectExecutor Get(EffectOperation operation)
    {
        if (!executors.TryGetValue(operation, out var executor))
        {
            throw new InvalidOperationException(
                $"No executor registered for {operation}"
            );
        }

        return executor;
    }
}
```

## 카드 정의

```csharp
public enum SubjectType
{
    Projectile,
    Enemy
}

public enum CardTier
{
    Common = 1,
    Uncommon = 2,
    Rare = 3,
    Legendary = 4,
    Mythic = 5
}
```

```csharp
[Serializable]
public sealed class DualEffectCardDefinition
{
    public string Id;
    public string DisplayNameKey;
    public CardTier Tier;
    public int ComputeCost;
    public string[] Tags;

    public EffectNode[] ProjectileEffects;
    public EffectNode[] EnemyEffects;

    public EffectLimits Limits;
    public CardUpgradeDefinition[] Upgrades;

    public string IconId;
    public string VfxId;
    public string SfxId;
}
```

```csharp
[Serializable]
public sealed class EffectNode
{
    public EffectOperation Operation;
    public EffectTarget Target;
    public EffectParameter[] Parameters;
    public EffectCondition[] Conditions;
    public EffectInheritancePolicy InheritancePolicy;
}
```

## 타워 정의

```csharp
[Serializable]
public sealed class TowerDefinition
{
    public string Id;
    public string DisplayNameKey;

    public TowerTrigger Trigger;
    public SubjectTypeMode SubjectTypeMode;
    public SubjectSelector Selector;

    public int SlotCount;
    public int ComputeCapacity;
    public float Cooldown;

    public EffectNode[] PassiveEffects;
    public string PrefabId;
}
```

ScriptableObject는 에디터 편집용으로 사용하되, 런타임에서는 불변 데이터로 변환한다.

문자열 ID는 로딩 시 정수 인덱스로 변환하여 반복적인 문자열 비교를 줄인다.

---

# 26. CraftPix 에셋 적용 규칙

CraftPix 에셋은 게임 규칙과 직접 연결하지 않는다.

게임 코드에서 실제 파일명이나 폴더명을 참조하지 말고 Asset Catalog를 사용한다.

예시:

```text
tower.ballista.basic
tower.cannon.basic
enemy.runner.walk
enemy.armored.death
effect.fire.small
effect.poison.cloud
ui.card.frame.common
```

## Fields 타일셋 및 스테이지 맵 규칙

Fields 원본은 다음 경로를 정식 Unity 임포트 위치로 사용한다.

```text
Assets/ThirdParty/CraftPix/Raw/Maps/Fields/
  Tiles/
    FieldsTileset.png
    FieldsTilesetTest.png
    FieldsTile_01.png ... FieldsTile_64.png
  Objects/
  Animated Objects/
```

루트의 임시 다운로드 폴더나 `Assets` 밖의 파일은 런타임과 Editor 생성기에서 직접 참조하지 않는다.

### 지형 타일 번호와 충돌 의미

* `FieldsTileset.png`은 게임에 표시하는 최종 `256x256` 아틀라스다.
* 각 타일은 `32x32`, PPU는 `32`, Filter Mode는 Point, 압축은 사용하지 않는다.
* 타일 번호는 좌측 상단이 1, 우측 하단이 64인 8열 행 우선 순서다.
* Unity Sprite Rect의 Y축은 아래에서 시작하므로 1번 Rect는 `(0, 224, 32, 32)`, 38번은 `(160, 96, 32, 32)`, 64번은 `(224, 0, 32, 32)`다.
* `FieldsTile_01.png`부터 `FieldsTile_64.png`까지의 개별 파일은 아틀라스 조각과 픽셀 단위로 동일해야 한다.
* `FieldsTilesetTest.png`은 표시용 이미지가 아니라 이동 가능 영역을 정의하는 의미 마스크다.
* 의미 마스크의 `#A6B04F` 초록 픽셀은 몬스터가 통과할 수 없고, `#EB9661` 주황 픽셀은 통과할 수 있다.
* 최종 아틀라스의 장식용 초록색 픽셀을 충돌로 오인하지 않는다. 충돌 마스크는 반드시 `FieldsTilesetTest.png`에서 빌드 시점에 굽고 런타임에 텍스처를 다시 스캔하지 않는다.
* 38번 타일은 1024픽셀 전체가 초록색인 완전 차단 타일이다.
* 11, 18, 20, 27번 타일은 완전 통과 타일이다.
* 부분 차단 타일은 32행 비트 마스크를 `FieldTerrainTile`에 저장하고 같은 마스크에서 Sprite Physics Shape를 생성한다.
* `TilemapCollider2D`와 `CompositeCollider2D`는 표시·물리 안전장치다. 결정적 적 이동의 권위 데이터는 계속 `GameLogic.PathModel`이며, 타일 충돌이 경로 진행도를 대신하지 않는다.

### 정적 오브젝트와 건설 지점

`Objects`의 9개 분류와 현재 변형 수는 다음과 같다.

| 분류 | 변형 수 |
| --- | ---: |
| Shadow | 6 |
| Fence | 10 |
| Pointer | 6 |
| Stone | 16 |
| Grass | 6 |
| Flower | 12 |
| Decor | 22 |
| Camp | 6 |
| Bush | 6 |

총 90개 오브젝트는 편집용 Tile/Palette 라이브러리로 모두 유지한다. 실제
Stage 장식에서 바이옴 바닥의 Grass, Flower, Dirt는 Tilemap을 사용한다.
다만 열린 초지의 꽃 군락과 작은 풀은 1유닛 셀 간격으로 흩어지지 않도록
`Decorative Biomes` 아래의 개별 `SpriteRenderer`로 생성한다. 나무, 부쉬,
울타리, 텐트, 돌, 표지판도 같은 개별 인스턴스 구조를 사용한다. 장식
오브젝트는 충돌 의미를 직접 소유하지 않으며, 오브젝트 외곽을 보고
몬스터 충돌을 새로 추론하지 않는다.

* 개별 장식은 1/32 월드 단위로 스냅한 연속 좌표를 사용한다.
* 같은 타일 영역에 여러 장식을 겹쳐 배치할 수 있다.
* 나무 수관, 부쉬, 울타리, 텐트는 결정적 시드로 `flipX` 변형을 섞는다.
* 장식은 전역 균등 분산하지 않고 숲, 야영지, 암석 지대, 길가 같은
  의미 구역 안에 집중한다.

* `PlaceForTower1.png`은 즉시 건설 가능한 지점이다.
* `PlaceForTower2.png`은 잠긴 지점이며, 현재 상태에서는 타워를 배치할 수 없다.
* 잠금 해제 비용은 `phase1-content.json`의 `buildSpotUnlockCosts`가 작성 원본이다. 프리팹이나 씬에 경제 규칙을 중복 하드코딩하지 않는다.
* 잠금 해제는 계획 단계에서만 가능하고, `GameSimulation`이 재화를 한 번 차감한 뒤 해당 지점을 사용 가능 상태로 바꾼다.
* 잠긴 지점을 누르면 해금 명령과 비용 안내를 제공하고, 해금 뒤에는 동일한 `TowerBuildSiteView`가 PlaceForTower1 표시로 전환한다.
* 건설 지점의 프레젠테이션 상태는 `Locked`, `Available`, `Occupied` 세 가지다.

### 애니메이션 오브젝트

모든 애니메이션 시트는 왼쪽에서 오른쪽으로 6프레임이며 기본 프레임 시간은 `0.12초`다.

Flag 방향은 다음 규칙을 사용한다.

| 원본 | 방향 | 좌우 반전 시 |
| --- | --- | --- |
| 1 | Down, 6시 | 사용하지 않음 |
| 2 | DownLeft, 7시 30분 | DownRight, 4시 30분 |
| 3 | Up, 12시 | 사용하지 않음 |
| 4 | UpLeft, 10시 30분 | UpRight, 1시 30분 |
| 5 | Left, 9시 | Right, 3시 |

2, 4, 5번만 같은 프레임을 X축 반전해 우측 방향 은행을 만든다. 반전 사본
PNG를 생성하지 않고 `SpriteRenderer.flipX`로 표현한다.

* Campfire 1은 불이 꺼진 연기 상태 애니메이션이다.
* Campfire 2는 불이 붙은 상태 애니메이션이다.
* 두 상태는 별도 `FieldAnimatedTile` 자산으로 프레임을 보존하며,
  Stage에서는 개별 `FieldSpriteAnimator`로 재생한다.

### 생성 에셋과 Stage01 편집 규칙

생성기는 다음 위치를 사용한다.

```text
Assets/Game/Data/Maps/Fields/
  Tiles/Terrain/
  Tiles/Props/
  Tiles/Animated/
  Palettes/
Assets/Game/Prefabs/Maps/Fields/TowerBuildSite.prefab
Assets/Game/Scenes/Battle/Stage01.unity
```

Unity 메뉴 `Ruleforge TD/Assets/Rebuild Fields Tilemap Content`는 원본 임포트 설정, 타일 에셋, 3개 Palette와 프리팹을 멱등적으로 갱신한다. 이미 존재하는 `Stage01.unity`는 사용자의 수동 Tilemap 작업을 보호하기 위해 일반 재생성에서 덮어쓰지 않는다. 생성 기준 맵으로 되돌릴 때만 `Ruleforge TD/Scenes/Rebuild Stage 01 (Overwrite)`를 사용하고 확인 대화상자를 거친다.

`Stage01`은 다음 계층을 유지한다.

```text
Stage 01
  Grid
    Terrain
    Ground Decals
  Decorative Biomes
    forest_west
    forest_north
    forest_ridge
    forest_east
    camp_northeast
    camp_southwest
    scrub_south
    meadow_west
    meadow_central
    meadow_east
    roadside_verges
  Navigation
  Tower Build Sites
  Main Camera
```

* Terrain은 의미 마스크와 Collider를 가진 지형 타일 전용이다.
* Ground Decals는 충돌 없는 Grass, Flower, Dirt 지면 장식 전용이다.
* 일반 장식과 애니메이션 장식은 모두 Bottom Center Pivot의 개별
  SpriteRenderer이며 Y 기준 결정적 Sorting Order를 사용한다.
* `1 Shadow` 자산은 투영 그림자가 아니라 발밑 풀 베이스다. Center Pivot을
  유지하고 나무, 부쉬, 텐트의 자식 Renderer로 연결해 본체 기준점과
  0.125 유닛 이내에 둔다. 한 셀 아래에 독립 배치하지 않는다.
* 숲의 나무 기준점은 서로 최소 간격을 두되 수관은 의도적으로 겹치게 한다.
  부쉬와 울타리는 숲 내부 또는 도로를 향한 숲 경계에 군집시킨다.
* 캠프는 텐트, 캠프파이어, 상자, 통나무, 부서진 울타리를 하나의 의미
  구역으로 구성하며 텐트끼리 약하게 겹칠 수 있다.
* 열린 초지는 전역 확률로 꽃을 한 송이씩 흩뿌리지 않는다. 결정적 시드로
  3개 이상의 `Wildflower Meadow` 구역과 내부 패치 중심을 만들고,
  각 패치에 여러 꽃을 0.2유닛 이상의 간격으로 모아 배치한다.
* 초지별 꽃 팔레트는 3종 이하로 제한하고, 모든 꽃은 1유닛 이내에 같은
  군락의 이웃을 가져야 한다. 작은 풀은 보조 밀도로 섞고 돌은 초지당
  최대 1개만 외곽에 배치해 꽃 수가 돌 수의 8배 이상이 되게 한다.
* 초지 장식은 길, 건설 지점, 기존 숲·캠프·암석 바이옴의 확장 경계를
  피하며, 꽃 위치·종류·좌우 반전과 돌 위치는 서로 독립된 시드를 쓴다.
* 길가 Flag와 Pointer는 경로 세그먼트의 법선 방향으로 배치하고 도로
  중심부에는 놓지 않는다.
* 건설 지점 검증은 기준점 거리만 보지 않고 본체와 발밑 베이스의 합성
  Bounds가 건설 패드와 겹치지 않는지 확인한다.
* 장식 밀도는 고유 타일 셀 수로 평가하지 않는다. 최소 인스턴스 수,
  의미 구역 수, 숲 수관 겹침, 좌우 반전 다양성으로 검증한다.
* `StagePathAuthoring`의 웨이포인트와 건설 지점 좌표는 `phase1-content.json`과 일치시킨다.
* 생성기 검증은 64개 타일 순서, 90개 오브젝트 자산, 10개 애니메이션
  자산, 38번 완전 차단, 경로 여유 폭, 바이옴 구성, 건설 지점 수와 잠금
  상태, 씬 Collider 구성을 확인한다.
* 새 장식은 전역에 임의로 흩어 놓지 않고 생성기의 바이옴 정의와 결정적
  시드를 통해 추가한다. Palette는 원본 자산 확인과 지면 타일 편집에 쓴다.

## Archer Tower 스프라이트 및 애니메이션 규칙

Archer Tower 원본은 다음 경로를 정식 Unity 임포트 위치로 사용한다.

```text
Assets/ThirdParty/CraftPix/Raw/Towers/Archer/
  Idle/
  Upgrade/
  Units/
    1/
    2/
    3/
    Arrow/
```

루트 폴더 등에 받은 원본이 있더라도 런타임이나 Editor 생성기가 `Assets` 밖의 파일을 직접 참조하지 않는다.

### 타워 본체

* `Idle/<Level>.png`과 `Upgrade/<Level>.png`의 Level은 1부터 7까지이며, 1이 최하 단계, 7이 최상 단계다.
* 모든 타워 셀은 `70x130`이고 한 행에 있으며, 애니메이션은 왼쪽에서 오른쪽으로 진행한다.
* `Upgrade/1`은 빈 공사 상태에서 1단계를 건설한다.
* `Upgrade/N`은 `N-1`단계에서 `N`단계로 변하는 애니메이션이다.
* Upgrade는 모든 단계가 4프레임이다.
* Idle 프레임 수는 단계별로 `1, 4, 4, 6, 6, 6, 6`이다. 전 단계를 4프레임으로 가정하지 않는다.
* Upgrade 마지막 프레임을 계속 표시하지 않고, 재생 완료 후 목표 단계의 `Idle/<Level>` 첫 프레임으로 전환한다.
* 타워 셀 Pivot은 Bottom Center `(0.5, 0)`를 사용하여 모든 단계의 바닥 위치를 고정한다.
* Idle은 첫 프레임을 별도로 오래 유지하지 않는다.
* 모든 Idle 프레임은 프레임당 `0.12초`의 동일한 속도로 왼쪽에서 오른쪽까지 계속 반복한다.
* 타워별 랜덤 시작 지연이나 랜덤 첫 프레임 유지 시간을 적용하지 않는다. 깃발과 장식이 계속 자연스럽게 움직이도록 모든 타워가 동일한 반복 규칙을 사용한다.
* 1단계 Idle은 한 프레임뿐이므로 정지 상태를 유지한다.

### 타워 위 궁수

궁수 시트 이름은 적과 같은 방향 규칙을 따른다.

```text
<Direction first letter>_<Behaviour>.png
```

* 방향은 `D` Down, `U` Up, `S` Side다.
* 행동은 `Idle`, `Preattack`, `Attack`을 사용한다.
* 모든 궁수 셀은 `48x48`, 한 행, 왼쪽에서 오른쪽 순서다.
* Idle은 4프레임, Preattack은 1프레임, Attack은 6프레임이다.
* 궁수 발 위치에 맞춰 기본 Pivot은 bottom-left 기준 `(24px, 15px)`, normalized `(0.5, 0.3125)`를 사용한다.
* 수평 이동 또는 조준에는 `S_<Behaviour>`를 사용한다.
* 현재 `S` 원본은 왼쪽을 향한다. 오른쪽은 `SpriteRenderer.flipX = true`로 표현한다.
* 위/아래에는 좌우 반전을 적용하지 않는다.
* 방향 판정은 적과 동일하게 수평 절댓값이 수직 절댓값보다 클 때 Side를 선택하고, 나머지는 Up 또는 Down을 선택한다.
* 공격 상태 흐름은 `Idle → Preattack → Attack → Idle`이다.
* 화살 발사 시점은 zero-based Attack frame 4, 즉 다섯 번째 프레임 진입 시점이다.

열린 타워의 기본 궁수 수는 다음과 같다.

* 1단계: 1명
* 2단계: 2명
* 3단계: 2명
* 5단계: 2명
* 6단계: 3명

닫힌 지붕 단계도 실제 공격을 수행하는 내부 궁수를 가진다.

* 4단계: 내부 궁수 2명, Unit Tier 2
* 7단계: 내부 궁수 3명, Unit Tier 3

내부 궁수 GameObject와 방향/공격 애니메이터는 활성 상태로 유지하되 `SpriteRenderer.enabled = false`로 숨긴다. 루트 GameObject 자체를 비활성화하면 Attack 발사 프레임 이벤트가 멈추므로 금지한다. 4단계는 로컬 발사구 `(-0.20, 0.61), (0.20, 0.61)`, 7단계는 `(-0.28, 0.72), (0, 0.72), (0.28, 0.72)`를 사용하여 화살이 건물의 창구에서 나오게 한다.

궁수 `SpriteRenderer`는 타워 본체보다 높은 Sorting Order를 사용하여 항상 타워 앞에 렌더링한다. 기본값은 타워 본체 `20`, 궁수 `21`이며, 화살은 이보다 높은 순서를 사용한다.

궁수는 난간 양 끝에 걸치지 않도록 타워 중앙에 대칭으로 모아 배치한다. 기본 수평 위치는 1명일 때 `0`, 2명일 때 `-0.16, 0.16`, 3명일 때 `-0.23, 0, 0.23`이다.

난간 위에 걸쳐 보이지 않도록 단계별 기본 좌석 높이는 로컬 Y 기준 `1단계 0.78`, `2단계 0.94`, `3단계 1.14`, `5단계 1.23`, `6단계 1.18`을 사용한다. 인원별 시각적 겹침을 피하기 위한 미세 오프셋만 이 기준 높이에 더한다.

Upgrade 재생 중에는 궁수를 숨긴다. 건설 애니메이션이 끝나 목표 Idle로 전환되면 궁수는 좌석보다 `1.65` 유닛 위에서 나타나 약 `0.34초` 동안 아래로 가속 낙하하고, 짧은 착지 반동 후 정확한 좌석 위치에 고정된다. 여러 명은 `0.055초` 간격으로 차례로 떨어진다. 이 연출은 Rigidbody나 전투 난수를 사용하지 않는 렌더링 전용 결정적 애니메이션이며, 낙하 중에는 공격하지 않는다.

원본에는 7개 타워 단계와 3개 궁수 Tier 사이의 명시적 연결 데이터가 없으므로 다음 매핑을 프로젝트 기본값으로 사용한다.

* 타워 1~2단계: Unit Tier 1
* 타워 3~5단계: Unit Tier 2
* 타워 6~7단계: Unit Tier 3

모든 단계의 궁수들은 해당 단계에 매핑된 동일 Unit Tier를 사용한다. 닫힌 단계의 내부 궁수는 낙하 연출을 재생하지 않는다.

### Archer Arrow 방향 은행

`Units/Arrow/1.png`부터 `27.png`까지는 순서대로 재생하는 시간 애니메이션이 아니다.

다음 세 묶음은 서로 다른 크기의 방향 스프라이트 은행이다.

* `1~13`: 큰 화살, Up에서 Right까지 13방향
* `14~22`: 중간 화살, Up에서 Right까지 9방향
* `23~27`: 작은 화살, Up에서 Right까지 5방향

각 은행에서 번호가 증가할수록 Up에서 Right 방향으로 회전한다. 실제 발사 벡터의 절댓값 각도에 가장 가까운 스프라이트를 선택하고, 왼쪽은 `flipX`, 아래쪽은 `flipY`로 나머지 사분면을 표현한다.

프로젝트 기본 외형 매핑은 성장감을 위해 다음과 같이 사용한다.

* Unit Tier 1: 작은 화살 `23~27`
* Unit Tier 2: 중간 화살 `14~22`
* Unit Tier 3: 큰 화살 `1~13`

Arrow 파일은 크기가 서로 다른 개별 Single Sprite이므로 Grid로 슬라이스하지 않는다. 파일명을 문자열로 정렬하면 `1, 10, 11...` 순서가 되므로 반드시 숫자로 파싱해 정렬한다.

### Archer Tower 전투 쇼케이스

`ArcherTowerAnimationTest`는 실제 적 프리팹 `Slime`, `Bee`, `Dog`, `Goblin`을 중앙 적 통로에 미리 배치한다. 각 적은 기존 `EnemyHealth`, `DirectionalEnemyAnimator`, `EnemyTestActor`를 그대로 사용하며, 좌우 왕복 이동 중 실제 이동 벡터에 맞는 방향 애니메이션을 표시한다.

쇼케이스 궁수는 사거리 안의 가장 가까운 살아 있는 적을 결정적으로 선택한다. 궁수 방향과 화살의 초기 방향 및 추적 지점은 모두 `적 transform + Y 0.35`인 몸 중심 조준점을 사용한다. 화살은 선택한 적을 추적하고 명중 시 `EnemyHealth.TakeDamage`를 호출한다. 적이 사망하면 이동을 멈추고, `1.8초` 후 원래 통로 중심에서 체력과 이동 상태를 초기화해 다시 등장한다.

쇼케이스의 단계별 기본 발사 간격은 `1단계 3.00초`, `2단계 2.60초`, `3단계 2.25초`, `4단계 1.95초`, `5단계 1.65초`, `6단계 1.35초`, `7단계 1.05초`다. 단계가 오를 때마다 반드시 간격이 짧아져야 하며, 약 `0.70초`인 전체 공격 애니메이션보다 짧게 설정하지 않는다. 화살 이동 속도는 전 단계 `8.5 units/s`를 사용한다.

각 궁수의 발사 이벤트는 단일 화살 1발만 만들며, 화살 자체를 분열하지 않는다. 동시에 날아가는 공격 애니메이션을 수용하도록 타워별 최대 12개 화살 풀을 사용하고, 물리 검색이나 프레임마다의 전역 객체 검색에 의존하지 않는다. 빌더는 처음 활성화된 루트 적 4마리와 비활성 분열 시드 풀 8마리를 준비한다. 쇼케이스 타워는 고정 배열이 아니라 전투 시스템의 결정적 대상 목록을 재사용 버퍼에 선형 복사해 이후 늘어난 분열체도 할당 없이 공격 대상으로 선택한다. 이 전투는 아트와 방향/타격 연출 검증용 프레젠테이션 로직이며, 실제 게임의 권위 있는 전투 시뮬레이션 규칙을 대신하지 않는다.

### Archer Tower 카드 효과 쇼케이스

Archer Tower 전투 쇼케이스의 기본 장착 프로그램은 왼쪽에서 오른쪽으로 `[SPLIT] → [BURN] → [POISON]`이며, 명중한 적을 Subject로 삼아 세 카드의 적 해석을 실행한다. 카드 정의와 수치의 작성 원본은 `Assets/Game/Data/Logic/phase1-content.json`이며, 런타임에서는 검증을 마친 불변 `CompiledContent`만 읽는다. 쇼케이스 코드, 프리팹 또는 씬에 같은 카드 수치를 별도 상수로 복제하지 않는다.

화살은 목표에 한 번 명중한 뒤 소멸하고, 명중 프록시가 해당 적에게 `[SPLIT] → [BURN] → [POISON]`을 순서대로 실행한다. 분열은 명중한 적을 총 2개체로 만들고 각 개체의 최대 체력과 현재 체력을 분열 직전 값의 `45%`, 즉 `4500 bps`로 설정한다. 분열 직전 적이 가진 화상과 중독을 포함한 모든 상태 런타임은 자식에게 깊은 복제되며, 이후 두 결과가 서로 독립적으로 틱한다. 두 결과는 같은 실행의 남은 카드인 화상과 중독도 이어서 받지만 분열 카드를 처음부터 다시 실행하지 않는다. 분열이 성공할 때마다 두 결과의 크기는 직전 크기의 `90%`가 된다.

씬은 최대 체력과 현재 체력이 각각 `1000`인 `Slime`, `Bee`, `Dog`, `Goblin` 루트 적 4마리로 시작하고, 각 루트마다 같은 프리팹으로 만든 비활성 분열 자식 2마리를 시드 풀로 준비한다. 초기 풀은 8마리, 초기 대상은 12마리다. 루트와 풀의 모든 개체는 `DirectionalEnemyAnimator`, `EnemyTestActor`, `EnemyHealth`, `ArcherEnemyCardStatusView`를 동일하게 구성하며, 하나의 `EnemyTestMovementSystem`이 활성화된 개체만 결정적 30Hz 이동으로 갱신한다.

적 분열에는 횟수나 가계 개체 수 상한을 별도로 두지 않는다. 다음 최대 체력 또는 현재 체력을 정수 내림으로 계산했을 때 하나라도 `1` 미만이면 원본을 변경하지 않고 그 분열만 중단한다. `1000` 체력과 `45%` 기준 성공 수열은 `450 → 202 → 90 → 40 → 18 → 8 → 3 → 1`이며 다음 결과 `0`에서 종료된다. 시드 풀이 모자라면 해당 가계의 완전한 적 템플릿을 결정적 순번으로 추가해 high-water 풀을 확장하고, 사망이나 가계 리셋 때 파괴하지 않고 재사용한다. 호환 설정 경로처럼 시드 풀이 없는 경우에는 해당 가계의 루트 적을 최초 템플릿으로 사용한다. 리셋은 루트 체력 `1000`, 세대 0, 원래 크기와 빈 상태로 복구하고 모든 자식을 같은 초기 상태의 비활성 풀로 돌려놓는다.

현재 `phase1-content.json`의 적 해석 기준 화상과 중독 수치는 다음과 같다.

* 화상: `ApplyBurn`, `amount 450 milli`, `durationTicks 90`, `intervalTicks 15`, `maxStacks 10`. 30Hz 기준 3초 동안 0.5초 간격으로 틱하며 `radiusMilli 1500`은 적 해석의 전염 반경이다.
* 화상 탄환의 `BindBurn`은 `amount2 1000 milli` 길이마다 불길 선분을 확정하고, `amount3 60 ticks`에 따라 각 선분을 2초간 유지한다. 작성 중 선분은 매 이동 틱 탄환 위치까지 늘어나며, 마지막 선분은 적중점 또는 소멸점에서 닫혀 발사점부터 끝점까지 빈 구간 없이 이어진다.
* 불길 접촉 판정은 `GameSimulation`의 선분 Hazard가 담당한다. 같은 불길 조각은 같은 적에게 한 번만 화상을 적용하고, 맞닿은 조각 여러 개가 같은 틱에 같은 적을 중복 적용하지 않는다. Unity 표현 계층은 `HazardSnapshot`을 단일 배치 메시로 애니메이션하며 별도 물리 판정을 만들지 않는다.
* 중독: `ApplyPoison`, `amount 500 milli`, `durationTicks 180`, `intervalTicks 30`, `maxStacks 20`. 30Hz 기준 6초 동안 1초 간격으로 틱하며, 현재 `chanceBps 5000` 값은 중독 피해의 방어 무시율로 컴파일된다.

쇼케이스의 `EnemyHealth`가 정수 체력을 사용하더라도 각 화상 및 중독 틱을 즉시 올림하여 1 피해로 바꾸지 않는다. 대상과 상태 종류별로 milli 피해를 누적하고, 누적값이 `1000 milli` 이상일 때만 정수 피해를 적용한 뒤 나머지를 보존한다. 이 누적기는 표시 프레임 속도가 아니라 고정된 상태 틱 일정에 따라 갱신한다.

적 상태 배지는 체력바 주변에 활성 상태와 중첩 수를 `B×N`, `P×N`처럼 표시한다. 배지는 `CompiledContent`에서 파생된 현재 상태를 읽는 표현 요소일 뿐, 중첩 수명이나 피해를 자체적으로 판정하지 않는다. 화상은 주황 계열, 중독은 녹색 배경과 겹치지 않는 보라색 계열을 기본으로 사용하며, 비활성 상태의 배지는 숨긴다.

적이 사망 후 재등장할 때는 체력과 이동만 초기화하지 않는다. 리스폰 경로에서 상태 프록시의 `ClearAll`을 먼저 호출하여 화상과 중독의 중첩, 남은 시간, 다음 틱, milli 피해 나머지, 상태 배지와 잔류 시각 효과를 모두 제거한다. 이전 생명의 지속 피해가 새로 등장한 적에게 이어지면 안 된다.

이 카드 쇼케이스는 단일 화살 명중을 적 분열, 상태 배지, 지속 피해 표현으로 연결하는 프레젠테이션 프록시다. 저장, 리플레이, 보상, 웨이브 진행, 가계 예산 및 실제 전투 판정의 권위는 계속 `GameSimulation`과 적 Subject를 제공하는 `Mutation Obelisk` 규칙에 있다. 쇼케이스 프록시는 이를 대체하거나 별도의 카드 의미를 정의하지 않는다.

Archer Tower 7단계는 현재 `ballista`의 프레젠테이션 에셋이다. 별도의 게임 로직 타워 ID나 업그레이드 규칙을 억지로 추가하지 않으며, 실제 업그레이드 시뮬레이션이 도입될 때 Asset Catalog를 통해 연결한다.

## 권장 폴더 구조

```text
Assets/
  ThirdParty/
    CraftPix/
      Raw/
      Sprites/
      Tiles/
      Effects/
      UI/
      Licenses/

  Game/
    Runtime/
      Core/
      Simulation/
      Combat/
      Cards/
      Towers/
      Enemies/
      StatusEffects/
      Rewards/
      Waves/
      Rendering/
      UI/

    Data/
      Cards/
      Towers/
      Enemies/
      Waves/
      Balance/
      AssetCatalog/

    Prefabs/
      Towers/
      Enemies/
      Projectiles/
      Effects/
      UI/

    Scenes/
      Bootstrap/
      MainMenu/
      Battle/

    Editor/
      AssetImport/
      DataValidation/
      BuildTools/

    Tests/
      EditMode/
      PlayMode/
```

## 픽셀 아트 임포트 기준

* Texture Type: Sprite
* Sprite Mode: Multiple 또는 Single
* Filter Mode: Point
* Mip Maps: Off
* Wrap Mode: Clamp
* Compression: None 또는 픽셀 품질을 해치지 않는 설정
* Pixels Per Unit: 프로젝트 전체 기준으로 통일
* Pivot: 캐릭터와 적은 Bottom Center 권장
* 타일은 Grid 기준과 정확히 일치
* Sprite Atlas 사용

팩마다 프레임 크기가 다를 수 있으므로 에셋 팩별 ImportProfile을 만든다.

가능하면 Editor 스크립트로 다음 작업을 자동화한다.

* Sprite Sheet 자동 슬라이싱
* Pivot 설정
* Pixels Per Unit 설정
* Sorting Layer 설정
* 애니메이션 클립 생성
* Asset Catalog 등록
* 누락된 스프라이트 검증

수십 개의 AnimatorController를 수동으로 만들지 않는다.

간단한 픽셀 애니메이션은 데이터 기반 SpriteAnimationPlayer를 사용해도 된다.

---

# 27. 성능 규칙

## Object Pool

다음 개체는 반드시 풀링한다.

* 탄환
* 적
* 파티클
* 폭발 효과
* 상태이상 아이콘
* 데미지 텍스트
* 체력바

## 공간 탐색

모든 타워가 매 틱 전체 적 목록을 순회하지 않는다.

간단한 Spatial Hash Grid를 사용한다.

용도:

* 사거리 내 적 탐색
* 폭발 범위 탐색
* 도탄 대상 탐색
* 전염 대상 탐색
* 적 충돌 탐색
* 자석 및 흡수 대상 탐색

## 업데이트 구조

* 각 탄환마다 무거운 Update를 사용하지 않는다.
* 각 상태이상마다 Coroutine을 만들지 않는다.
* 상태이상은 중앙 StatusSystem에서 처리한다.
* 타워 쿨다운은 중앙 TowerSystem에서 처리한다.
* 매 프레임 LINQ 사용을 피한다.
* 반복되는 문자열 연결을 피한다.
* 게임 루프에서 불필요한 List 및 배열 생성을 피한다.
* 이벤트 객체를 필요 이상으로 생성하지 않는다.
* 전투 로그는 제한된 원형 버퍼를 사용한다.

## 성능 목표

데스크톱 브라우저:

* 적 200개
* 탄환 500개
* 상태이상 1,000개 이상
* 평균 60FPS 목표

모바일 브라우저:

* 평균 30FPS 이상
* 파티클 수 자동 감소
* 데미지 텍스트 수 제한
* 화면 밖 이펙트 생략

개발 모드에 성능 오버레이를 제공한다.

표시할 정보:

* FPS
* 시뮬레이션 틱 시간
* 활성 적 수
* 활성 탄환 수
* 활성 상태이상 수
* 처리된 이벤트 수
* 현재 최대 연쇄 깊이
* 오브젝트 풀 사용량
* 공간 검색 소요 시간
* 가장 오래 걸린 시스템

---

# 28. UI 및 UX

## 전투 화면

상단:

* 현재 웨이브
* 골드
* 본진 체력
* 전투 속도
* 일시정지 버튼

중앙:

* 게임 맵
* 적 이동 경로
* 타워
* 탄환
* 상태이상 및 전투 효과

우측 또는 하단:

* 선택한 타워 정보
* 타워 문장
* 카드 슬롯
* 연산력
* 타워 업그레이드
* 카드 설명

## 타워 문장 UI

예시:

공격할 때 탄환이

1. 두 개로 분열하고
2. 표식이 있는 적을 추적하며
3. 적중 시 화상을 부여합니다.

아래에는 계산된 결과를 표시한다.

* 예상 탄환 수
* 탄환당 피해
* 최대 관통 수
* 최대 도탄 수
* 예상 상태이상 중첩
* 최대 연쇄 깊이
* 연산력 5 / 6

## 카드 표시

카드는 다음 정보를 보여준다.

* 이름
* 티어
* 연산 비용
* 카드 태그
* 탄환 효과
* 적 효과
* 현재 타워에서 적용되는 효과
* 강화 레벨
* 발동 제한

카드를 타워 위에 드래그하면 현재 적용될 해석을 강조한다.

예시:

분열

탄환 효과:
탄환이 두 발로 나뉩니다.

적 효과:
적이 두 개체로 나뉩니다.

현재 발리스타 타워 위에 드래그했다면 탄환 효과를 강조한다.

## 실행 흐름

타워를 선택하면 실행 흐름을 표시한다.

```text
공격
  ↓
탄환 생성
  ↓
분열 ×2
  ↓
추적
  ↓
적중
  ↓
화상
  ↓
폭발
```

## 전투 로그

개발 모드 또는 상세 화면에서 다음을 확인할 수 있다.

```text
Ballista Tower attacked.
Projectile created.
Split executed.
Two child projectiles created.
Projectile A hit Enemy 12.
Burn applied: 2 stacks.
Enemy 12 died.
Death Engine triggered.
Explosion affected 4 enemies.
```

사용자 표시 로그는 자연어로 변환하되, 내부 이벤트 ID와 체인 ID는 디버그 모드에서 확인할 수 있게 한다.

---

# 29. 저장 시스템

저장 시스템을 인터페이스로 추상화한다.

```csharp
public interface ISaveRepository
{
    void Save<T>(string key, T value);
    bool TryLoad<T>(string key, out T value);
    void Delete(string key);
}
```

저장 대상:

* 설정
* 음량
* 그래픽 품질
* 도감
* 해금 카드
* 해금 타워
* 메타 성장
* 최근 사용한 빌드
* 런 기록
* 데이터 버전

저장 데이터에는 버전 번호를 포함한다.

데이터 구조가 변경되면 마이그레이션할 수 있어야 한다.

---

# 30. 테스트 필수 항목

## EditMode 테스트

### 분열

* 탄환이 정확히 2개로 분열하는지
* 적이 정확히 2개체로 분열하는지
* 적 보상 예산이 증가하지 않는지
* 생성된 개체가 남은 카드만 실행하는지
* 두 결과의 체력이 1 미만일 때만 분열이 중단되는지
* 분열 시 모든 상태이상이 깊은 복제되고 두 가지가 독립적으로 틱하는지
* 분열 세대마다 원본과 자식 크기가 각각 10% 감소하는지

### 카드 순서

* 분열 → 화상
* 화상 → 분열

두 결과가 서로 다른지 확인한다.

### 골드

* 복제체가 추가 골드를 생성하지 않는지
* CardBounty 골드가 경제 타워를 재발동하지 않는지
* 웨이브당 골드 발동 제한이 적용되는지

### 재귀

* 한 연쇄에서 1회만 실행되는지
* 연쇄 깊이 제한이 적용되는지

### 상태이상

* 화상과 중독의 틱이 정확한지
* 둔화 최대치가 지켜지는지
* 출혈이 이동 및 밀치기에 반응하는지
* 보스 제어 효과가 저항 게이지로 전환되는지

### 결정성

* 같은 시드와 같은 입력에서 같은 결과가 나오는지
* 카드 드래프트 결과가 재현 가능한지

## PlayMode 테스트

* 타워 배치
* 카드 드래그 앤 드롭
* 웨이브 시작 및 종료
* 적 경로 이동
* 탄환 적중
* 보스 등장
* 승리 및 패배
* 전투 속도 변경
* 일시정지
* 저장 및 불러오기

## WebGL 빌드 검증

각 주요 단계에서 다음을 확인한다.

* 브라우저에서 정상 로딩
* 콘솔 반복 오류 없음
* 입력 정상 작동
* 카드 드래그 정상 작동
* 오디오 재생 정상
* 저장 데이터 정상
* 장시간 실행 시 메모리 증가가 과도하지 않음
* 비활성 탭 복귀 후 게임 상태 정상

---

# 31. 개발 문서

프로젝트 시작 시 다음 문서를 작성한다.

* README.md
* GAME_DESIGN.md
* ARCHITECTURE.md
* CARD_RULES.md
* TOWER_RULES.md
* EFFECT_PIPELINE.md
* STATUS_RULES.md
* REWARD_RULES.md
* BALANCE_RULES.md
* ASSET_PIPELINE.md
* WEBGL_BUILD.md
* TESTING.md
* CONTENT_CATALOG.md

중요한 규칙을 변경하면 코드뿐 아니라 문서도 함께 수정한다.

---

# 32. 개발 단계

## Phase 0 — 프로젝트 기반

구현 항목:

* Unity 2D 프로젝트 구성
* WebGL 빌드 타깃 설정
* Bootstrap, MainMenu, Battle 씬 구성
* 게임 진입 흐름
* 기본 UI
* Seeded Random
* 고정 시뮬레이션 틱
* EventQueue
* Object Pool
* 기본 테스트 환경
* 데이터 검증 도구
* CraftPix ImportProfile
* 브라우저 테스트 빌드

완료 조건:

* 빈 전투 맵이 브라우저에서 실행됨
* 일시정지와 속도 변경이 작동함
* 에디터와 WebGL에서 동일한 시뮬레이션 틱이 작동함

---

## Phase 1 — 핵심 수직 슬라이스

타워:

* 발리스타 타워
* 뮤테이션 오벨리스크
* 데스 엔진

카드:

* 분열
* 관통
* 화상
* 둔화
* 폭발
* 밀치기
* 표식
* 골드 획득
* 중독
* 거대화
* 축소
* 기절

적:

* Raider
* Runner
* Armored Knight

콘텐츠:

* 맵 1개
* 경로 1개
* 건설 지점 8개
* 5웨이브
* 간단한 정예 적 1종
* 카드 드래프트
* 카드 장착
* 카드 순서 변경
* 전투 로그
* 승리 및 패배 화면

가장 먼저 검증할 조합:

발리스타 타워:

[공격할 때]
[탄환이]
[분열]
[화상]
[폭발]

뮤테이션 오벨리스크:

[적이 사거리에 들어오면]
[그 적이]
[분열]
[화상]
[폭발]

데스 엔진:

[적이 사망하면]
[주변 적이]
[분열]
[화상]
[폭발]

완료 조건:

* 세 타워에서 동일 카드가 서로 다르게 작동함
* 플레이어가 UI를 통해 차이를 이해할 수 있음
* 분열된 적의 총 보상이 증가하지 않음
* 연쇄 폭발이 정상 작동함
* 무한 루프가 발생하지 않음
* WebGL 브라우저 빌드에서 정상 작동함

---

## Phase 2 — 기본 게임 완성

추가 타워:

* 캐논 바스티온
* 리피터 아웃포스트
* 배리어 게이트
* 펄스 모놀리스
* 알케미 랩
* 듀얼리티 코어

추가 카드:

* 도탄
* 저주
* 속박
* 에어본
* 감전
* 빙결
* 잔상
* 전염
* 봉인
* 부식
* 환원
* 처형

추가 기능:

* 12웨이브
* 중간 보스
* 최종 보스
* 카드 강화
* 타워 업그레이드
* 런 결과 통계
* 빌드 태그 분석
* 저장 및 불러오기
* 그래픽 품질 설정
* 사운드 설정

---

## Phase 3 — 희귀 및 전설 시스템

추가 카드:

* 복제
* 희생
* 역행
* 공명
* 흡수
* 시간 정지
* 변이
* 기생
* 환생
* 연쇄
* 재귀
* 역순 실행
* 이중 해석
* 과잉 복제
* 마지막 명령

추가 타워:

* 오비탈 코어
* 워든 스파이어
* 아케인 포지
* 헥스파이어 스파이어
* 릴레이 아카이브
* 소울 리액터

추가 기능:

* 새로운 맵
* 신규 적
* 보스 3종 이상
* 카드 도감
* 타워 도감
* 일일 시드
* 도전 모드

---

## Phase 4 — 신화 및 출시 준비

추가 카드:

* 특이점
* 불사조 코어
* 시간 균열
* 거울 세계
* 우로보로스

추가 기능:

* 메타 성장
* 난이도 단계
* 업적
* 모바일 브라우저 대응
* 터치 조작
* 접근성 옵션
* 성능 자동 품질 조절
* 빌드 크기 최적화
* 브라우저별 테스트
* 오류 로깅
* 저장 데이터 마이그레이션
* 최종 밸런싱

---

# 33. MVP 완료 기준

MVP는 다음 조건을 모두 만족해야 한다.

* 브라우저에서 실행 가능
* Unity WebGL로 렌더링
* 최소 3개 타워 사용 가능
* 최소 12개 카드 사용 가능
* 카드 드래그 앤 드롭 가능
* 카드 순서 변경 가능
* 같은 카드가 탄환과 적에게 다르게 작동
* 적 분열 후 전체 보상량이 증가하지 않음
* 처치 연쇄와 폭발 연쇄가 작동
* 실제 무한 루프가 없음
* 현재 타워의 행동 문장이 자연어로 표시됨
* 카드 발동 흐름을 확인할 수 있음
* 새 카드가 기존 전투 시스템 수정 없이 추가 가능
* 새 타워가 기존 카드 수정 없이 추가 가능
* 핵심 시스템에 자동 테스트 존재
* 에디터 오류 없음
* C# 컴파일 오류 없음
* WebGL 빌드 오류 없음
* 브라우저 콘솔에 반복 오류 없음

---

# 34. 코딩 규칙

1. 카드 효과를 타워나 탄환 MonoBehaviour에 직접 하드코딩하지 않는다.
2. 게임 규칙과 렌더링 코드를 분리한다.
3. 카드와 타워는 데이터 중심으로 정의한다.
4. 신규 효과는 EffectRegistry를 통해 추가한다.
5. 한 파일이 지나치게 커지지 않게 책임별로 분리한다.
6. 게임 루프에 LINQ와 과도한 메모리 할당을 사용하지 않는다.
7. 게임 플레이 난수는 Seeded Random만 사용한다.
8. 효과 수치는 Balance 데이터에서 관리한다.
9. 분열, 복제, 재귀를 구현할 때 연쇄 예산 테스트를 작성한다.
10. 골드 효과를 구현할 때 RewardOrigin을 반드시 확인한다.
11. 전투 중 모든 상태이상을 개별 Coroutine으로 구현하지 않는다.
12. 핵심 규칙을 Unity Inspector 설정에만 의존하지 않는다.
13. 프리팹 연결이 필요한 경우 검증 도구를 작성한다.
14. CraftPix의 실제 파일명을 게임 규칙에서 직접 사용하지 않는다.
15. 에디터에서만 정상 작동하는 상태를 완료로 판단하지 않는다.
16. 저장소 파일을 변경한 모든 작업은 완료 보고 전에 전체 게임 WebGL 빌드를
    새로 생성한다. Phase 종료나 대규모 기능 작업에만 한정하지 않으며,
    소규모 버그 수정, UI 변경, 데이터·설정·문서 변경도 빌드를 생략하지
    않는다. 런타임 동작에 영향을 주는 변경은 브라우저에서도 검증한다.
17. TODO만 남긴 상태에서 기능을 완료했다고 보고하지 않는다.
18. 구현이 애매하면 “동일 카드의 이중 해석” 원칙을 우선한다.
19. 범용 중세·마법 판타지 스타일을 유지한다.

---

# 35. 첫 번째 작업 지시

현재는 전체 게임을 한꺼번에 만들지 말고 Phase 0과 Phase 1만 구현한다.

작업 순서:

1. 현재 저장소 구조를 확인한다.
2. Unity 2D 프로젝트 구조를 생성하거나 정리한다.
3. WebGL 빌드 타깃을 설정한다.
4. GAME_DESIGN.md와 ARCHITECTURE.md를 작성한다.
5. Bootstrap, MainMenu, Battle 씬을 구성한다.
6. 고정 시뮬레이션 틱을 구현한다.
7. Seeded Random을 구현한다.
8. EventQueue와 ChainBudget을 구현한다.
9. EffectRegistry와 EffectExecutor 구조를 구현한다.
10. 카드 데이터 구조를 구현한다.
11. 타워 데이터 구조를 구현한다.
12. 발리스타 타워를 구현한다.
13. 뮤테이션 오벨리스크를 구현한다.
14. 데스 엔진을 구현한다.
15. 분열 카드를 구현한다.
16. 화상 카드를 구현한다.
17. 폭발 카드를 구현한다.
18. 탄환 분열과 적 분열의 차이를 자동 테스트한다.
19. 적 보상 예산 보존 규칙을 구현한다.
20. 카드 장착 및 순서 변경 UI를 구현한다.
21. CraftPix 에셋 ImportProfile을 만든다.
22. 기본 타워, 적, 탄환 스프라이트를 Asset Catalog에 연결한다.
23. 5웨이브 전투를 구현한다.
24. 전투 로그를 구현한다.
25. 승리 및 패배 화면을 구현한다.
26. EditMode 테스트를 실행한다.
27. PlayMode 테스트를 실행한다.
28. WebGL 빌드를 생성한다.
29. 브라우저에서 직접 실행해 오류를 수정한다.
30. 구현 결과와 현재 제한 사항을 문서에 기록한다.

그래픽이 단순하거나 임시 에셋이어도 괜찮다.

첫 번째 목표는 그래픽 완성도가 아니라 다음 세 문장이 실제로 서로 다르게 동작하는지 검증하는 것이다.

발리스타 타워:

공격할 때 탄환이
→ 분열하고
→ 화상을 부여하며
→ 폭발한다.

뮤테이션 오벨리스크:

적이 사거리에 들어오면 그 적이
→ 분열하고
→ 화상 상태가 되며
→ 사망 시 폭발한다.

데스 엔진:

적이 사망하면 주변 적이
→ 분열하고
→ 화상 상태가 되며
→ 사망 시 폭발한다.

이 세 조합이 서로 다르게 작동하고, 플레이어가 UI에서 그 차이를 이해할 수 있어야 한다.

---

# 36. 작업 완료 보고 형식

작업을 마치면 다음 형식으로 보고한다.

## 구현 완료

* 구현한 시스템
* 구현한 카드
* 구현한 타워
* 구현한 UI
* 적용한 CraftPix 에셋

## 생성 또는 수정한 파일

* 파일 경로
* 파일 역할
* 주요 변경 내용

## 테스트 결과

* EditMode 테스트
* PlayMode 테스트
* 실패한 테스트와 수정 내용

## WebGL 빌드 결과

* 빌드 성공 여부
* 브라우저 실행 여부
* 브라우저 콘솔 오류
* 확인한 성능 지표

## 실행 방법

* Unity 에디터 실행 방법
* Battle 씬 실행 방법
* WebGL 빌드 방법
* 로컬 웹 서버 실행 방법

## 알려진 제한 사항

* 아직 구현하지 않은 기능
* 임시 그래픽
* 임시 밸런스
* 성능상 주의점

## 다음 우선순위

* 다음 단계에서 구현할 기능 3~5개

---

# 37. 테스트 및 검증 실행 정책

이 저장소에서 빌드, 테스트, 린트, 타입 검사, 의존성 설치, Docker 작업, 배포 검증, 상태 확인, 서버 점검 등 실행 및 검증 작업을 수행할 때는 반드시 `$spark-test` 스킬을 사용한다.

실행 순서는 다음과 같다.

1. 관리형 Spark 서브에이전트를 먼저 시도하지 않는다.
2. `$spark-test` 스킬의 CLI 실행기를 즉시 사용한다.
3. Unity 테스트와 WebGL 빌드는 라이선스 IPC와 UPM 소켓 접근이 필요하므로
   CLI 실행기의 sandbox를 `danger-full-access`로 지정한다.
4. 저장소 파일을 변경한 작업은 관련 EditMode 또는 PlayMode 테스트를
   실행한 뒤 반드시 전체 게임 WebGL 빌드를 새로 생성한다. 테스트 통과만으로
   작업을 완료하지 않으며, 사용자에게 별도 요청을 다시 받기 전까지 빌드를
   미루거나 생략하지 않는다.
5. WebGL 빌드는
   `RuleforgeTD.Editor.AssetImport.MainMenuSceneBuilder.BuildWebGLFromCommandLine`
   을 사용하고 `Builds/WebGL/RuleforgeTD`의 게시 산출물을 갱신한다.
6. WebGL 빌드 성공 후 `Tools/serve_stage01.py`로
   게시한 WebGL 산출물을 반드시 `0.0.0.0:8767`에서 서빙한다. `8767`은
   이 저장소의 고정 완료 포트이며 임의의 대체 포트를 사용하지 않는다.
   이미 이 저장소의 서버가 사용 중이면 해당 서버를 유지한 채 갱신된
   산출물이 응답하는지 다시 검증한다. 다른 프로세스가 점유 중이면 그
   프로세스를 임의로 종료하지 않고 완료 차단 사유로 보고한다. 이 서버는
   작업 완료 후에도 유지해 사용자가 Tailscale로 접속할 수 있게 한다.
7. Spark는 로컬 URL과 현재 머신의 Tailscale IPv4 URL에 각각 HTTP
   요청을 보내 `index.html`과 WebGL 필수 산출물이 정상 응답하는지
   확인한다. Tailscale 접속 주소는
   `http://<tailscale-ip>:<actual-port>/`를 사용한다.
8. Tailscale가 설치되어 있지 않거나 연결되지 않아 외부 확인이
   불가능하면 이를 성공으로 보고하지 않고 미해결 차단 사유로 남긴다.
9. 메인 에이전트는 Spark가 수행한 환경 변경 명령을 중복 실행하지 않는다.
10. 완료 보고에는 실행 경로(`Spark CLI`), 실행 명령 또는 검증 범위,
   종료 상태, 주요 출력, 변경된 파일이나 서비스, WebGL 산출물 경로와
   Tailscale 확인 URL, HTTP 검증 결과, 미해결 경고를 포함한다.
11. `$spark-test`로 이미 위임받아 실행 중인 Spark 또는 Luna 작업자는
   `$spark-test`나 다른 Codex 작업자를 다시 호출하지 않고, 위임받은
   명령과 검증을 현재 작업 안에서 직접 수행한다.

읽기 전용 점검은 기본적으로 변경 없이 수행한다. 저장소 수정, 의존성 설치, 배포, 재시작 등 상태를 바꾸는 작업은 사용자가 허용한 범위 안에서만 수행한다.
