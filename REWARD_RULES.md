# Ruleforge TD 골드와 보상 규칙

## 1. 핵심 불변식

적을 분열, 복제, 부활시키는 행위만으로 다음 총량이 증가해서는 안 된다.

- 가계의 최대 골드 예산
- 이미 지급되지 않은 골드 예산
- 웨이브 완료 기여도
- 처치 통계의 원본 스폰 수

카드팩 처치 진행도만 예외적으로 분열 직전 가지 값의 51%를 원본과 자식이 각각 가진다. 이 보너스는 적 분열 세대 8과 가계 누적 개체 256의 결정적 상한 안에서만 발생하며 골드와 웨이브 완료 기여도에는 적용하지 않는다. 정상 웨이브 편성이 단일 카드팩 임계값에 도달 가능한지는 회귀 테스트로 별도 고정한다.

위험 보상 카드인 거대화, 가속, 골드 획득, 금단의 거래만 데이터에 정의된 방식으로 예산을 늘릴 수 있다. 증가도 가계 원장을 거쳐야 하며 카드/타워/웨이브 상한을 적용한다.

현재 Phase 1 런타임에서 보상 증액을 실제로 실행하는 카드는 거대화와 골드 획득 두 장이다. 가속과 금단의 거래는 전체 카드 설계 계약에만 있으며 후속 Phase 구현 대상이다.

## 2. 가계 원장

웨이브에서 직접 스폰된 적마다 고유 `LineageId`와 원장 하나를 만든다.

| 필드 | 의미 |
| --- | --- |
| `LineageId` | 원본 스폰 가계 ID |
| `BaseRewardBudget` | 웨이브 데이터가 부여한 기본 골드 |
| `MaxRewardBudget` | 허용된 위험 보상 증액을 포함한 총 상한 |
| `PaidReward` | 이미 지급한 골드 |
| `ForfeitedReward` | 본진 도달/무보상 제거로 소멸한 골드 |
| `ReservedByBranch` | 살아 있는 가지별 미지급 할당 |
| `ProgressBudget` | 원본 스폰의 웨이브 기여도 단위 |
| `ConsumedProgress` | 사망/도달/제거로 정산한 기여도 |
| `BaseCardPackProgress` | 원본의 카드팩 처치 진행도 |
| `AwardedCardPackProgress` | 처치로 누적 바에 지급한 진행도 |
| `ForfeitedCardPackProgress` | 탈출한 가지와 함께 소멸한 진행도 |
| `AppliedAugmentKeys` | 같은 보상 증액의 중복 적용 방지 키 |
| `SplitCount` | 이 가계에서 성공한 전체 분열 연산 수 |
| `SpawnedEntityCount` | 가계 생성 한도 검사용 누적 개체 수 |

항상 다음 식이 성립해야 한다.

```text
PaidReward + ForfeitedReward + 살아 있는 가지 할당
  = MaxRewardBudget
```

정수 반올림 잔여분도 원장에 남겨야 하며 사라지거나 새로 생기면 안 된다.

`SimulationSnapshot.Lineages`는 LineageId 오름차순으로 공개된다. 각 `LineageSnapshot`에는 최고 세대, 전체 분열 수, 누적 생성 수, 생존 수, 기본/최대/지급/몰수 보상, 전체/소비 진행도, 적용된 보상 증액 키 수가 들어간다. 가지별 현재 할당은 각 `EnemySnapshot`의 `RewardBudget`, `WaveProgressBudget`으로 확인한다.

## 3. 분열 할당

적이 N개 가지로 분열할 때 분열 직전 그 적의 미지급 할당을 N등분한다.

1. `quotient = allocation / N`
2. `remainder = allocation % N`
3. 가지를 `EntityId` 오름차순으로 정렬한다.
4. 앞의 `remainder`개 가지에 1씩 더 준다.

원본 가지가 기존 EntityId를 유지하고 새 가지보다 작으므로 홀수 잔여분은 원본부터 받는다.

예:

```text
부모 할당 10 → 두 가지 5, 5
부모 할당 9  → 두 가지 5, 4
```

웨이브 기여도도 같은 알고리즘으로 분배한다. 원본 스폰의 `ProgressBudget`은 적 데이터가 정하며, 분열 후 모든 가지의 합은 원래 값과 같다. Phase 1 일반 적은 1, 정예 골렘은 5를 사용하므로 1인 일반 적이 둘로 분열하면 EntityId가 작은 원본 가지에 1, 추가 가지에 0이 배정된다.

카드팩 진행도는 이 보존 분배 대신 각 결과에 5,100 basis point를 곱한다. 10,000은 첫 분열에서 5,100과 5,100, 같은 가계의 두 번째 분열에서 5,100과 2,601과 2,601이 된다. 탈출한 가지는 자신의 할당을 전부 몰수하며 복제·환생·보스 소환은 새 카드팩 진행도를 만들지 않는다.

## 4. 카드팩 진행과 특수 몬스터

- 일반 적 처치: 10,000
- 정예 적 처치: 30,000
- 누적 출현 임계값: 3,500,000(런당 1회)
- 보스, 소환물, 복제체, 특수 몬스터: 0

임계값을 넘긴 처치가 일반 적이면 그 정의를, 정예 처치이면 현재 웨이브의 첫 일반 적 정의를 사용해 특수 몬스터를 만든다. 특수 몬스터는 체력 150%, 속도 120%, 크기 110%이고 골드·웨이브·카드팩 진행도와 누출 피해가 모두 0이다.

특수 몬스터를 분열시킨 경우 가계의 모든 가지가 사망해야 팩 하나를 지급한다. 한 가지라도 누출되면 기회는 사라진다. 특수 몬스터 또는 미개봉 특수 팩이 있는 동안 다음 특수 몬스터를 만들지 않으며 초과 진행도는 유지한다.

## 5. 복제, 환영, 부활

- 독립적으로 처치 가능한 복제체가 보상을 준다면 부모의 미지급 할당을 분열과 같은 방식으로 나눠야 한다.
- 체력을 공유하고 직접 보상을 주지 않는 유령 복제체는 할당 0을 받으며 부모 할당을 유지한다.
- 거울 세계 환영, 시간 잔상, 소울 잔재는 보상과 웨이브 기여도 0이다.
- 부활은 같은 가지와 할당을 유지한다. 최초 사망 시 보상을 지급하지 않고 최종 사망 또는 영구 제거에서 한 번만 정산한다.
- 환생 영혼처럼 카드에 “추가 보상 없음”이 명시된 별도 개체는 0을 받는다. 원본의 보상이 이미 지급됐다면 다시 열지 않는다.

## 6. 사망과 본진 도달

적 사망이 최종 확정되면 해당 가지에 예약된 골드를 한 번 지급하고 `PaidReward`에 더한다. 부활할 수 있으면 지급을 보류한다.

적이 본진에 도달하면 해당 가지 할당을 지급하지 않고 `ForfeitedReward`로 옮긴다. 디버그 제거나 웨이브 강제 종료도 명시된 정책에 따라 지급 또는 몰수 중 하나를 선택하며, 원장에 흔적 없이 버리지 않는다.

웨이브 기여도는 사망, 본진 도달, 합법적 제거 모두에서 소비한다. 따라서 분열로 제거해야 할 개체 수는 늘어나도 웨이브 전체 기여도는 늘지 않는다.

## 7. 보상 증액

### 이어하기 스테이지의 몬스터 기본 드롭

첫 스테이지의 적 보상은 콘텐츠와 엘리트 특성 적용 결과를 그대로 쓴다. 이어하기로 다음 스테이지에 진입할 때마다 각 예약 몬스터의 기본 드롭 예산에 `floor(sqrt(직전 스테이지 보상))`를 적용한다. 골드는 정수이므로 양수 결과는 최소 1을 보존한다.

```text
10골드 → 스테이지 2에서 3골드 → 스테이지 3에서 1골드
```

이 제곱근 규칙은 `EnemyDrop`의 최초 가계 예산에만 적용한다. 골드 획득 카드 등 이후의 보상 증액은 축소된 가계 예산을 기준으로 기존 상한을 적용하며, 카드 바운티·연속 처치·정예 추가 보너스·웨이브 완료 보너스는 제곱근 처리하지 않는다.

보상 증액은 다음 순서를 따른다.

1. 카드가 `RewardAugmentRequest`를 생성한다.
2. `(LineageId, SourceTowerId, CardInstanceId, AugmentKind)` 키가 이미 적용됐는지 확인한다.
3. 카드별/타워별/웨이브별 증액 상한을 확인한다.
4. 증가량을 `MaxRewardBudget`과 대상 가지 할당에 원자적으로 더한다.
5. 키와 증가량을 원장에 기록한다.

Phase 1에서 적 해석의 거대화와 골드 획득은 같은 카드 인스턴스/타워 조합으로 한 가계에 한 번만 증액한다. 분열 뒤 여러 가지에 같은 프로그램이 실행되어도 첫 성공만 증액한다.

축소는 rewardBudget을 줄이지 않는다. 플레이어가 이미 감수하거나 획득한 보상 가능성을 카드 순서 때문에 잃지 않게 하기 위함이다.

## 8. RewardOrigin

모든 골드 변화는 출처를 가진다.

```csharp
public enum RewardOrigin
{
    EnemyDrop,
    CardBounty,
    TowerTrigger,
    ShopRefund,
    Debug,
    KillStreak,
    EliteBonus,
    WaveCompletion
}
```

| 출처 | 예시 | 경제 트리거 허용 |
| --- | --- | --- |
| `EnemyDrop` | 적 최종 사망 보상 | 허용 |
| `CardBounty` | 골드 획득 카드, 금단의 거래 생존 보상 | 같은 카드/경제 타워 재트리거 금지 |
| `TowerTrigger` | 경제 타워가 직접 만든 골드 | 같은 경제 트리거 금지 |
| `ShopRefund` | 상점 환불 | 데이터가 명시할 때만 허용 |
| `Debug` | 개발 명령 | 항상 금지 |
| `KillStreak` | 짧은 시간 안에 원본 가계를 연속 처치한 소액 보너스 | 금지 |
| `EliteBonus` | 기본 정예 또는 엘리트 특성 보유 적 처치 보너스 | 금지 |
| `WaveCompletion` | 웨이브 완료 보너스 | 금지 |

경제 트리거 여부는 “골드 값이 바뀌었다”만 보지 않고 `RewardOrigin`과 `RootChainId`를 함께 확인한다.

## 9. Phase 1 골드 획득 카드

탄환 해석은 새로운 적 첫 적중 시 소량의 `CardBounty`를 예약한다.

- “새로운 적”은 탄환의 `HitLedger`에 EntityId가 없는 적이다.
- 같은 적을 관통, 되돌아오기, 겹침으로 다시 맞혀도 추가 지급하지 않는다.
- 탄환당 3회와 타워당·웨이브당 20골드 한도를 적용한다.
- 한도에 막힌 사건은 골드를 지급하지 않는다. Phase 1은 이 정상적인 한도 도달을 안전 예산 위반 진단으로 취급하지 않는다.

적 해석은 가계 rewardBudget을 늘린다.

- 증액은 최종 사망 시 `EnemyDrop`으로 지급된다.
- 분열과 복제는 늘어난 총량도 다시 나눌 뿐 복제하지 않는다.
- 같은 가계에 같은 카드 인스턴스/타워 조합은 한 번만 적용한다.

### 고밀도 웨이브의 소액 보상

- Raider/Runner/Armored Knight/Elite Golem 기본 처치 골드는 각각 3/2/7/18이다.
- 45틱(1.5초) 안에 서로 다른 원본 가계를 연속 처치하면 5번째마다 1골드를 준다.
- 기본 정예 또는 엘리트 특성 보유 가계를 처치하면 3골드를 추가한다.
- 웨이브 완료 보너스는 `8 + 2 × 완료 웨이브 인덱스(0부터)`다.
- 엘리트 특성 배율과 정수 올림까지 반영한 예약 적 처치 골드는 1,078이다. 여기에 완료 보너스 144, 정예 보너스 최대 150, 연속 처치 보너스를 더해 정상 전멸 기준 총 사용 가능 골드를 이전 924의 약 1.5배 수준으로 맞춘다.

엘리트 특성의 `rewardMultiplierBps`는 원본 가계의 최초 `RewardBudget`에 한 번만 적용한다. 골드 예산은 정수이므로 양의 소수 결과를 올림해 100%보다 큰 배율을 가진 엘리트가 낮은 기본 보상 때문에 일반형과 같은 골드를 주지 않게 한다. 이후 분열체는 이 증가된 예산을 나누며, 복제·환생·환영·시간 잔상·거울 세계는 새 예산을 만들지 않는다.

연속 처치와 정예 보너스는 개체 사망 수가 아니라 `LineageId`당 한 번만 경제 원장에 반영한다. 따라서 분열·복제·환생·환영으로 실제 처치 표시가 늘어도 총 경제 보너스는 늘지 않는다. HUD의 연속 처치 수와 한 RootChain의 처치 수는 전투 쾌감용 실제 개체 통계이므로 경제 원장과 분리한다.

골드 획득 카드의 이번 웨이브 생성량과 타워별 남은 한도는 `CombatTelemetrySnapshot`에서 확인한다. 12틱 안의 여러 지급은 `골드 +N` 하나로 합산하며 개별 +1 팝업을 만들지 않는다.

## 10. 건설 경제

런에서 사용하는 재화는 골드 하나뿐이다. 시작 골드는 0이며 `run.freeInitialTowerCount`가 정한 초기 배치 수만 무료다. 이후 건설비는 각 타워 정의의 `constructionCost`와 동일 종류 배치 수에 따른 `duplicateCostStepBps`로 계산한다. 업그레이드비는 해당 타워의 다음 `levels[].upgradeCost`다. 구형 전역 건설비는 두지 않으며 건설과 업그레이드 모두 타워별 콘텐츠가 단일 원본이다. 모든 건설 지점은 시작부터 열려 있다. 판매·환불·카드 구매·카드 강화 비용은 현재 범위에 없다.

배치와 업그레이드 요청은 GameLogic의 권위 있는 견적을 사용한다. 견적은 대상 정의/인스턴스, 현재 레벨, 최대 레벨, 비용, 현재 잔액 기준 구매 가능 여부를 함께 반환한다. 명령은 같은 규칙을 다시 검증한 뒤 한 번에 골드를 차감하고 상태를 변경한다. 어느 검증이든 실패하면 골드와 상태 해시가 바뀌지 않는다. UI와 헤드리스 리플레이는 비용이나 최대 레벨을 별도로 계산하지 않는다.

## 11. 처치 귀속과 전투 통계

Phase 1 사망 처리 이벤트는 처치를 만든 상위 EventId와 SourceTower/Card를 보상·사망 바인딩 처리 동안 전달한다. 지속 피해 처치도 상태의 원본 SourceTower/Card를 이 이벤트에 전달한다. `CombatTelemetrySnapshot`은 카드별 피해/발동, 현재·최고 연속 처치, RootChain 최대 처치, 폭발·감전·도탄·전염, 최근/웨이브/카드 골드를 웨이브 단위로 제공한다. `EnemyState`와 공개 스냅샷에는 `KillingEventId`나 마지막 피해 출처를 영속 저장하지 않는다.

통계 계약은 다음을 구분한다.

- `OriginalSpawnDefeated`: 원본 가계가 최종 정산된 횟수
- `EntityDeaths`: 분열체를 포함한 실제 사망 개체 수
- `RewardPaid`: 실제 지급 골드
- `RewardAugmented`: 위험 카드로 늘어난 골드
- `ChainKills`: RootChain 안에서 최초 피해 대상 외의 사망 수

분열체 사망 수를 웨이브의 원본 적 처치 수로 잘못 더하지 않는다.

## 12. 결정성과 원자성

- 같은 틱의 여러 사망은 Death 이벤트 순서로 처리한다.
- 보상 지급과 가계 원장 갱신은 하나의 원자 작업이다.
- 골드가 부족한 금단의 거래처럼 소비와 효과가 결합된 경우 소비 예약 성공 후에만 효과를 등록한다.
- 정수 분배의 나머지는 항상 EntityId 오름차순으로 배정한다.
- 원장 불변식이 깨지면 릴리스에서도 추가 지급을 중단하고 진단 오류를 남긴다.

## 13. 필수 테스트

- 10골드 적을 2회 분열해도 전체 지급은 10을 넘지 않는다.
- 홀수 골드와 progress unit의 잔여분이 결정적으로 분배된다.
- 복제/환영/부활이 새 rewardBudget을 만들지 않는다.
- 거대화/골드 증액이 한 가계에 같은 출처로 중복 적용되지 않는다.
- `CardBounty`와 `TowerTrigger`가 아케인 포지를 재발동하지 않는다.
- 본진에 도달한 가지의 골드는 몰수되고 원장 합계는 보존된다.
- 같은 시드/명령에서 지급 순서와 최종 골드가 동일하다.
- 같은 가계의 분열체 여러 기를 처치해도 연속 처치/정예 경제 보너스는 한 번만 지급된다.
- 12틱 안의 여러 골드 지급은 표시만 합산되고 권위 골드 값과 출처별 원장은 각각 보존된다.
- 카드팩 진행도 10,000의 두 단계 분열이 5,100과 2,601 정수값을 정확히 만든다.
- 무료 초기 배치 수 이후에는 선택한 타워의 건설 견적만 정확히 차감한다.
- 같은 종류 타워의 단계별 할증과 다음 레벨 업그레이드 비용이 콘텐츠와 견적에서 일치한다.
