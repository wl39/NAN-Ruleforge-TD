# Ruleforge TD 픽셀 VFX 스타일 가이드

## 목표

CraftPix 원본 스프라이트의 해상도와 선명도는 유지하면서 카드 VFX만 저해상도 Point 필터로 렌더링한 것처럼 보이게 한다.

참고 방식은 저해상도 RenderTexture를 Point 필터로 확대하는 Unity 렌더링 기법이다. Ruleforge TD에서는 전체 카메라를 저해상도로 내리지 않고, VFX의 절차형 도형만 같은 시각 규칙으로 래스터화한다.

## 기본 규칙

- VFX 좌표 그리드: `0.0625` 월드 유닛
- 최소 픽셀 크기: `0.0625 × 0.0625` 월드 유닛
- 곡선 표현: 부드러운 선이 아니라 그리드에 맞춘 사각 픽셀의 연속
- 색상 보간: 시작색과 종료색 사이를 최대 4단계로 양자화
- 알파 보간: `0`, `0.25`, `0.5`, `0.75`, `1`의 5단계
- 효과당 최대 픽셀 블록: `112`
- 동시 효과 풀: `64`
- 분열: 6개의 독립된 사각 스파크 사용
- 기본 재생 반경: `0.36` 월드 유닛
- 크기 예외: 분열과 흡혈은 원래 반경 유지
- 재생 기준점: 적중 또는 사망한 적의 `WorldImpactCenter`
- 추적: 0.56초 재생 동안 같은 적 개체의 이동·반동·사망 위치를 계속 따라감

## 성능 규칙

- 픽셀마다 GameObject를 생성하지 않는다.
- 효과 하나는 동적 Mesh 하나만 사용한다.
- 정점·색상·UV·인덱스 배열은 풀 생성 시 한 번 할당하고 재사용한다.
- VFX는 게임 로직 상태를 변경하지 않는다.
- 풀 상한에 도달하면 기존 정책대로 가장 오래된 효과 슬롯을 재사용한다.

## 적용 범위

현재 공용 카드 실행 VFX 전체와 분열 스파크에 적용한다. 적 스프라이트, 타워 스프라이트, 지형과 UI는 원본 픽셀 해상도를 유지한다.

## 범위 판정 표시

- 판정 원: 257×257 Point 필터 절차형 스프라이트
- 외곽선 두께: 4텍셀
- 최대 알파: `0.92`
- 내부 채움: 없음
- 크기: 표현용 기본 반경이 아니라 시뮬레이션 `radiusMilli / 1000f`
- 전용 애니메이션: 판정 원과 별도 레이어로 재생하며 가장 바깥 픽셀까지 실제 반경 안에 둔다.
