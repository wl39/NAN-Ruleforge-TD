# Ruleforge TD 타워 규칙

## 1. 타워의 책임

타워는 카드 효과를 직접 정의하지 않는다. 타워는 문장의 앞부분인 실행 문맥만 제공한다.

이 문서의 18종은 전체 게임 설계 카탈로그다. 현재 Phase 1 런타임에 컴파일된 타워는 `ballista`, `mutation_obelisk`, `death_engine` 세 종뿐이다.

```text
Trigger
→ SubjectType
→ SubjectSelector
→ ProgramSnapshot
```

타워 정의는 최소한 다음 정보를 가진다.

| 필드 | 의미 |
| --- | --- |
| `Id` | 저장 가능한 안정적인 문자열 ID |
| `DisplayNameKey` | 표시 문자열 키 |
| `Trigger` | 카드 프로그램을 시작하는 조건 |
| `SubjectTypeMode` | Projectile, Enemy, Alternating, Inherited |
| `Selector` | Subject와 보조 타깃 선택 규칙 |
| `SlotCount` | 장착 가능한 물리 슬롯 수 |
| `ComputeCapacity` | 장착 카드 ComputeCost 합계 상한 |
| `CooldownTicks` | 기본 발동 간격 |
| `TriggerLimits` | 대상별/웨이브별/연쇄별 재발동 제한 |
| `PassiveProgram` | 카드보다 먼저 컴파일되는 타워 고유 효과 |

UI용 Prefab ID, 스프라이트, VFX는 게임 로직 정의에 포함하지 않는다.

## 2. 공통 장착 규칙

- 장착 카드의 `SlotCost` 합은 `SlotCount`를 넘을 수 없다.
- 장착 카드의 `ComputeCost` 합은 `ComputeCapacity`를 넘을 수 없다.
- 빈 슬롯은 실행 시 건너뛴다.
- 계획 단계에서만 장착, 이동, 순서 변경을 허용한다.
- 웨이브 시작 시 장착 카드 ID와 인스턴스 ID를 타워별 프로그램 배열로 복사한다. 문서에서 이 웨이브 고정 복사본을 `ProgramSnapshot`이라 부른다.
- 타워 업그레이드가 슬롯이나 연산력을 바꾸더라도 진행 중인 웨이브의 스냅샷은 바뀌지 않는다.
- 카드가 없는 타워도 기본 공격/트리거 패시브는 실행하지만 빈 카드 프로그램은 사건을 추가하지 않는다.

## 3. 공통 타깃 선택

타워별 Selector가 다른 우선순위를 명시하지 않으면 살아 있고 공격 가능한 적을 다음 순서로 선택한다.

1. 해당 타워가 표식 우선을 지원하고 적이 표식 상태인지
2. 타워 또는 사건 중심과의 거리가 가까운 적
3. 경로 진행도가 큰 적
4. `EntityId`가 작은 적

범위 안의 모든 적을 고를 때도 이 순서로 정렬한 배열을 만든 뒤 각각 별도 Activation을 실행한다. 공간 해시 반환 순서는 결과에 영향을 주지 않는다.

새로 생성된 적은 생성된 같은 이벤트 안에서 “사거리에 새로 들어온 적”으로 다시 판정하지 않는다. 현재 틱의 공간 인덱스 갱신 후 다음 정상 판정부터 대상이 될 수 있으며, 부모가 가진 대상별 재발동 잠금은 가계 규칙에 따라 공유한다.

## 4. Phase 1 타워 상세 계약

런 시작 화면에는 `ballista`와 `mutation_obelisk`만 선택지로 제시된다. 둘 중 하나를 선택하면 `death_engine`은 `initiallyUnlockedTowers`에 의해 함께 소유되어, 빈 건설 지점이 있으면 같은 계획 단계부터 배치할 수 있다.

### 4.1 발리스타 타워 (`ballista`)

문장:

```text
[공격할 때] [탄환이] [카드 1] [카드 2] [카드 3]
```

- SubjectType: `Projectile`
- 슬롯/연산력: 3 / 5
- Trigger: 공격 쿨다운이 끝나고 사거리 안에 유효한 적이 있을 때
- Selector: 표식 우선, 거리, 경로 진행도, EntityId 순으로 한 적
- 기본 동작: 타깃 방향으로 기본 탄환 하나를 만들고 해당 탄환에 카드 프로그램을 실행한다.
- 프로그램 시점: 탄환의 기본 피해/속도/크기/수명을 설정한 직후, 첫 이동 전에 실행한다.
- 타깃 소실: 탄환이 추적 카드가 없으면 발사 방향을 유지한다. 추적 카드가 있으면 다음 정상 타깃 판정에서 재선택한다.
- 추천 카드: 분열, 관통, 화상

검증 조합 `[분열, 화상, 폭발]`은 두 탄환을 만들고, 두 탄환 모두 이후 적중 화상과 일회성 폭발 바인딩을 얻어야 한다.

### 4.2 뮤테이션 오벨리스크 (`mutation_obelisk`)

문장:

```text
[적이 처음 사거리에 들어오면] [그 적이] [카드 1] [카드 2] [카드 3]
```

- SubjectType: `Enemy`
- 슬롯/연산력: 3 / 5
- Trigger: 이전 타워 판정에서 범위 밖이던 적이 현재 판정에서 범위 안이 된 상승 에지
- Selector: 진입한 그 적
- 대상별 잠금: 같은 적/가계에 대한 재발동 대기시간을 둔다. 적이 범위를 나갔다 다시 들어와도 대기시간이 남으면 발동하지 않는다.
- 생성 방지: 범위 안에서 분열한 새 가지는 분열 자체를 새 진입으로 만들지 않는다. 부모의 해당 타워 재발동 잠금을 상속한다.
- 동시 진입: 정규 타깃 순서대로 각각 별도 Activation을 만든다.
- 추천 카드: 분열, 거대화, 표식

검증 조합 `[분열, 화상, 폭발]`은 진입한 적을 두 가지로 만들고, 두 가지 모두 이후 화상과 사망 폭발 바인딩을 얻어야 한다. 미지급 보상과 웨이브 기여도 총합은 변하지 않는다.

### 4.3 데스 엔진 (`death_engine`)

문장:

```text
[적이 사망하면] [주변 적이] [카드 1] [카드 2] [카드 3]
```

- SubjectType: `Enemy`
- 슬롯/연산력: 3 / 6
- Trigger: 데스 엔진의 감지 범위 안에서 적 사망이 최종 확정될 때
- Selector: 사망 위치의 효과 반경 안에 있는 살아 있는 모든 적. 방금 죽은 적은 제외한다.
- 발동 단위: 선택된 각 적마다 독립 Activation을 만들되 같은 RootChain과 사망 ParentEvent를 공유한다.
- 연쇄: 데스 엔진 효과로 다른 적이 죽으면 새 Death 사건이 다시 데스 엔진을 발동할 수 있다. 체인 깊이와 이벤트 예산을 우회할 수 없다.
- 다중 엔진: 여러 데스 엔진 범위에 사망이 겹치면 타워 EntityId 순으로 발동한다.
- 추천 카드: 폭발, 화상, 중독, 분열

검증 조합 `[분열, 화상, 폭발]`은 사망 주변의 각 적을 분열하고 두 가지에 화상과 사망 폭발 바인딩을 부여한다. 적용 시점에 즉시 폭발하지 않고 각 가지가 실제 사망할 때 폭발한다.

## 5. 전체 18개 타워 계약

아래 표는 후속 Phase까지의 Trigger/Subject 설계 계약이며, 표에 존재하는 것만으로 해당 타워가 현재 `CompiledContent`나 명령 대상에 등록되는 것은 아니다.

| ID | 타워 | Trigger | Subject / Selector | 슬롯 | 연산력 | 패시브 또는 제한 |
| --- | --- | --- | --- | ---: | ---: | --- |
| `ballista` | 발리스타 타워 | 공격할 때 | 생성한 기본 탄환 | 3 | 5 | 범용 단일 발사 |
| `cannon_bastion` | 캐논 바스티온 | 세 번째 공격마다 | 생성한 대포탄 | 4 | 7 | 큰 탄환, 약한 기본 폭발, 높은 밀치기 |
| `repeater_outpost` | 리피터 아웃포스트 | 같은 적을 세 번 공격하면 | 해당 적을 향한 추가 탄환 | 3 | 5 | 적별 연속 적중 카운터 |
| `beacon_spire` | 비콘 스파이어 | 적이 처음 사거리에 들어오면 | 진입 적을 향한 신호탄 | 3 | 5 | 적/가계별 첫 진입 기록 |
| `orbital_core` | 오비탈 코어 | 소유 탄환이 소멸하면 | 생성한 궤도 탄환 | 3 | 6 | 소멸 원인을 계승하되 동일 소멸 재트리거 제한 |
| `mutation_obelisk` | 뮤테이션 오벨리스크 | 적이 사거리에 들어오면 | 진입한 그 적 | 3 | 5 | 대상별 재발동 대기시간 |
| `barrier_gate` | 배리어 게이트 | 적이 결계선을 넘으면 | 결계선을 넘은 그 적 | 3 | 5 | 선 교차 방향과 적별 통과 시퀀스 기록 |
| `pulse_monolith` | 펄스 모놀리스 | 일정 시간마다 | 범위 안의 모든 적 | 2 | 5 | 범위 타워라 슬롯 2개 유지 |
| `death_engine` | 데스 엔진 | 감지 범위의 적이 사망하면 | 사망 위치 주변의 살아 있는 적 | 3 | 6 | 사망 연쇄 허용, 예산 강제 |
| `alchemy_lab` | 알케미 랩 | 적에게 서로 다른 상태 3종이 쌓이면 | 조건을 만족한 그 적 | 3 | 6 | 대상별 재발동 대기시간, 상태 ID 중복 제외 |
| `execution_gate` | 익스큐션 게이트 | 적 체력이 위에서 25% 이하로 내려갈 때 | 임계치를 넘은 그 적 | 3 | 6 | 적 하나당 한 번 |
| `warden_spire` | 워든 스파이어 | 정예/보스가 특수 능력을 사용하면 | 능력을 사용한 그 적 | 2 | 6 | 일반 적 사건 무시 |
| `arcane_forge` | 아케인 포지 | 허용 출처의 골드를 일정량 획득/소비하면 | 생성한 마법 탄환 | 4 | 7 | CardBounty/TowerTrigger 재트리거 금지 |
| `hexfire_spire` | 헥스파이어 스파이어 | 상태가 전염 또는 만료되면 | 생성한 마법 불꽃 탄환 | 3 | 6 | 같은 상태 사건의 중복 소비 방지 |
| `solar_core` | 솔라 코어 | 화상 피해 누적량이 임계치에 도달하면 | 생성한 태양 탄환 | 3 | 7 | 누적량 소비 후 나머지 이월 |
| `duality_core` | 듀얼리티 코어 | 홀수 발동은 공격, 짝수 발동은 탄환 적중 | 홀수 탄환 / 짝수 적중한 적 | 4 | 7 | 같은 카드 배열의 SubjectType을 교대 |
| `relay_archive` | 릴레이 아카이브 | 인접 타워가 마지막 카드를 완료하면 | 해당 인접 타워의 Subject와 타입 | 2 | 6 | 문맥 상속, 릴레이 간 재릴레이 제한 |
| `soul_reactor` | 소울 리액터 | 탄환 소멸 또는 적 사망 | 원본 타입의 잔재 Subject | 3 | 7 | 원본 타입 상속, 무보상 잔재 |

## 6. 후속 타워의 트리거 세부 규칙

### 카운터형

- 캐논의 세 번째 공격, 리피터의 세 번째 동일 적 적중, 듀얼리티의 홀짝 발동은 타워별 정수 카운터를 사용한다.
- 웨이브 시작 시 초기화하되 일시정지와 2배속은 카운터를 바꾸지 않는다.
- 리피터의 대상이 제거되면 해당 적 카운터를 제거한다. 분열한 새 가지는 부모의 적중 카운터를 기본 상속하지 않는다.

### 진입/교차형

- 비콘과 뮤테이션은 `outside → inside` 상승 에지에서만 발동한다.
- 배리어는 이전/현재 `PathProgress`가 결계 위치를 가로지른 경우 발동한다. 밀치기나 역행으로 반대 방향 교차도 유효하되 동일 틱 같은 선의 반복 교차는 한 번으로 합친다.
- 새로 생성된 적이 이미 범위 안에 있는 사실만으로 진입 사건을 만들지 않는다.

### 상태/경제형

- 알케미 랩은 활성 상태 ID가 3종이 되는 순간을 검출한다. 같은 상태의 여러 출처는 한 종류다.
- 헥스파이어는 상태 사건마다 한 번만 소비하며 전염과 만료를 구분하는 EventId를 기록한다.
- 솔라 코어는 실제 적용된 화상 피해만 누적한다. 면역이나 보호막으로 0이 된 피해는 누적하지 않는다.
- 아케인 포지는 `EnemyDrop`과 명시적으로 허용된 소비만 카운트한다. 자신이나 카드가 만든 `CardBounty`, `TowerTrigger`는 무시한다.

### 문맥 상속형

- 릴레이 아카이브는 발동한 인접 타워의 SubjectType, SubjectEntityId, RootChain을 그대로 이어받는다.
- 원 Subject가 이미 제거되었으면 발동을 취소하고 진단한다.
- Relay가 Relay를 다시 발동시키지 않도록 `relayConsumed` 토큰을 RootChain에 둔다.
- 소울 리액터의 적 잔재는 보상과 웨이브 기여도 0인 논리 Subject이며, 탄환 잔재는 충돌하지 않는 논리 탄환이다. 카드 프로그램이 새 유효 개체를 명시적으로 만들 때만 월드에 등장한다.

## 7. 타워 데이터 검증

현재 Phase 1 컴파일러는 다음 정의를 로딩 단계에서 거절한다.

- 안정 ID가 없거나 중복된다.
- Trigger, SubjectTypeMode, Selector 문자열이 등록된 enum 값이 아니다.
- 슬롯/연산력이 0 이하이거나 허용 상한을 넘는다.
- 쿨다운, 범위, 피해, 탄속, 탄환 수명, 선택 반경, 대상 수, 대상별 쿨다운이 허용 범위를 벗어난다.

현재 컴파일러는 Phase 1 JSON에 실제로 포함된 3종을 검증한다. 나머지 15종은 각 Phase에서 정의와 trigger executor가 추가된 뒤 런타임 검증 대상이 된다.

Trigger/Subject 조합의 의미 검증, 경제 출처 필터, Inherited 대상 소실 정책, 자체 재발동 제한은 해당 후속 타워가 실제 executor와 함께 들어올 때 추가해야 할 설계 조건이다.
