# Ruleforge TD 상태이상 규칙

## 1. 상태이상의 원본성

상태이상은 적 또는 탄환 MonoBehaviour, Coroutine, Animator 상태에 저장하지 않는다. Phase 1은 `GameSimulation` 내부의 중앙 상태 처리 루틴이 모든 상태 인스턴스를 고정 틱으로 갱신하며, 시뮬레이션 상태가 유일한 원본이다. 독립된 `StatusSystem` 클래스는 아직 분리하지 않았다.

현재 Phase 1 런타임은 화상, 중독, 둔화, 표식, 천공, 기절에 더해 Common·Uncommon 카드가 사용하는 도탄, 출혈, 추적 우선, 지연, 저주, 속박, 에어본, 감전, 냉기·빙결·빙결 면역, 잔상, 파동, 자석, 반사, 전염, 봉인, 부식, 회전, 흡혈, 공포·공포 반동 가속 상태를 처리한다. 이 중 일부 `StatusType`은 지속 피해가 아니라 결정적 실행과 표현을 위한 내부 표식이다.

상태 인스턴스는 최소한 다음 필드를 가진다.

| 필드 | 의미 |
| --- | --- |
| `Id` | 상태 정의 정수 ID |
| `InstanceId` | 런 안의 고유 인스턴스 ID |
| `SubjectEntityId` | 상태를 보유한 개체 |
| `SourceEntityId` | 마지막 또는 최초 직접 적용 개체 |
| `SourceTowerId` | 피해/보상 귀속 타워 |
| `SourceCardId` | 적용한 카드 |
| `Stacks` | 현재 중첩 |
| `Intensity` | 중첩과 별개인 효과 강도 |
| `DurationTicks` | 적용 시 기준 지속시간 |
| `ExpireTick` | 만료 예정 틱 |
| `MaxStacks` | 중첩 상한 |
| `RefreshPolicy` | 재적용 시 갱신 방식 |
| `TickInterval` | 지속 효과 간격 |
| `NextTick` | 다음 처리 틱 |
| `Tags` | 화염, 독, 제어, 약화 등 |
| `Inherited` | 명시적 생성 상속 가능 여부 |
| `Dispellable` | 해제 가능 여부 |

기본 병합 키는 `(StatusId, SourceTowerId, SourceCardId)`다. 같은 타워의 같은 카드에서 여러 탄환이 적용한 상태는 하나의 인스턴스에 중첩한다. 다른 타워 또는 다른 카드는 별도 인스턴스로 남겨 피해 귀속과 상호작용을 보존한다.

## 2. 적용과 갱신 순서

상태 적용은 다음 순서를 따른다.

1. Subject가 살아 있고 해당 상태를 받을 수 있는지 확인한다.
2. 상태 저항과 면역 변환을 적용한다.
3. 병합 키가 같은 기존 인스턴스를 찾는다.
4. 중첩과 Intensity를 상한 안에서 갱신한다.
5. RefreshPolicy에 따라 `ExpireTick`을 갱신한다.
6. 새 인스턴스라면 `NextTick = ApplyTick + TickInterval`로 예약한다.
7. Phase 1 표현 버퍼에 `StatusApplied`를 기록한다. 신규/갱신 구분이 필요한 규칙 이벤트는 후속 상태 트리거 Phase에서 추가한다.

적용한 틱에 지속 피해를 즉시 주지 않는다. 다음 틱이 만료 틱과 같으면 마지막 틱을 먼저 처리한 뒤 만료한다. Phase 1은 만료 상태를 즉시 제거하고 `StatusRemoved` 표현 이벤트를 기록한다. 헥스파이어가 활성화되는 Phase에서 자연 만료와 해제를 구분한 `StatusExpired` 규칙 이벤트를 추가한다.

## 3. RefreshPolicy

| 정책 | 동작 |
| --- | --- |
| `RefreshDuration` | 중첩을 올리고 만료를 현재 틱 기준 전체 지속시간으로 갱신 |
| `ExtendDuration` | 남은 시간에 정해진 연장량을 더하되 상한 적용 |
| `KeepLonger` | 기존 만료와 새 만료 중 늦은 값을 사용 |
| `ReplaceIfStronger` | 새 Intensity가 클 때만 강도와 지속시간 교체 |
| `Independent` | 병합하지 않고 별도 인스턴스 생성 |

Phase 1의 화상, 중독, 둔화, 표식은 `RefreshDuration`, 천공은 `KeepLonger`, 기절은 일반 적에게 `KeepLonger`를 기본으로 사용한다.

## 4. Phase 1 상태 상세 계약

구체 수치는 `BALANCE_RULES.md`와 밸런스 JSON이 최종 원본이다.

### 4.1 화상 (`status.burn`)

- 짧은 지속시간과 빠른 피해 틱을 가진다.
- 같은 출처의 적용은 중첩되고 지속시간을 갱신한다.
- 각 틱 피해는 현재 중첩과 Intensity를 사용하며 화염 저항을 적용한다.
- 화상 틱으로 적이 죽으면 원본 타워와 카드가 처치 기여 출처가 된다.
- Phase 1에서 최대 중첩 도달 자체는 자동 전염을 만들지 않는다. 전염 카드를 활성화한 Phase부터 전염 조건으로 사용한다.
- 탄환이 남기는 불길은 전역 활성 영역 2,048개 상한 안에서만 생성된다.

### 4.2 중독 (`status.poison`)

- 화상보다 길고 느린 지속 피해다.
- 같은 출처의 적용은 중첩되고 지속시간을 갱신한다.
- 방어력 일부를 무시하지만 독 저항은 적용한다.
- 높은 중첩 보너스는 데이터 곡선으로 정의하며 Phase 1 기본값은 선형 피해다.
- 탄환이 남기는 독안개는 불길과 같은 전역 활성 영역 2,048개 상한을 공유한다.

### 4.3 둔화 (`status.slow`)

- 이동 속도만 낮추며 공격/상태 틱 속도에는 영향을 주지 않는다.
- 각 출처의 감소율을 곱연산으로 결합한 뒤 최종 감소를 60%로 제한한다.
- 속박, 기절, 빙결과 동시에 존재할 수 있으며 지속시간은 정상적으로 흐른다.
- 강제 이동량은 둔화에 의해 줄지 않는다.

### 4.4 표식 (`status.mark`)

- 받는 피해 보정과 타깃 우선 플래그를 제공한다.
- 같은 출처는 최대 3중첩하며 지속시간을 갱신한다.
- 여러 출처의 피해 증가는 합산 후 전역 상한을 적용한다.
- 타깃 정렬은 표식 여부를 먼저 보며, 여러 표식의 개수로 추가 우선순위를 만들지 않는다.

### 4.5 천공 (`status.pierced`)

- 해당 적에게 적중한 탄환이 통과할 수 있도록 한다.
- 공격의 방어력 무시율을 늘린다.
- 같은 출처는 중첩하지 않고 더 긴 지속시간을 유지한다.
- 탄환의 전역 관통 상한과 이미 적중한 적 집합을 우회하지 않는다.

### 4.6 기절 (`status.stun`)

- 일반 적은 지속시간 동안 이동과 특수 행동을 할 수 없다.
- 피격, 화상, 중독 등 시간 기반 상태 처리는 계속된다.
- 정예와 보스에게는 상태 인스턴스를 직접 만들지 않고 제어 저항 게이지 기여로 변환한다.
- 저항 게이지가 임계치에 도달하면 짧은 `ControlInterrupt`를 적용하고 게이지를 0으로 만든다.

## 5. 정예와 보스 제어 저항

기절, 속박, 에어본, 강한 밀치기는 정예/보스에게 완전 면역으로 처리하지 않는다.

```text
제어 적용 요청
→ 카드별 ControlValue 계산
→ 대상 ControlResistanceMultiplier 적용
→ 저항 게이지 누적
→ 현재 임계치 도달 시 짧은 행동 방해
→ 게이지 초기화 및 Adaptation 증가
```

보스 기본 기준은 기절 1회당 게이지 30, 최초 임계치 100, 방해 24틱(0.8초)이다. 방해가 실제로 발동할 때마다 해당 웨이브 동안 임계치를 데이터의 `ControlGaugeStep`만큼 높여 최대 200까지 증가시킨다. Phase 1 정예 골렘의 Step은 25다. 즉 반복 제어는 계속 의미가 있지만 점차 더 많은 적용이 필요하다.

밀치기처럼 위치를 바꾸는 제어는 보스에게 실제 경로 이동 대신 ControlValue로 변환한다. 둔화, 저주, 피해 증폭은 강제 제어가 아니며 각각의 저항 데이터에 따라 정상 적용한다.

## 6. 이동과 상태의 상호작용

- 기절/빙결/시간 정지: 자발 이동과 특수 행동을 멈춘다.
- 속박: 자발 이동만 멈추며 피격과 특수 행동은 허용 가능하다.
- 에어본: 이동을 멈추지만 타깃/피격 가능 상태를 유지한다.
- 공포/역행: 이동 방향을 음수로 바꾼다.
- 밀치기: 한 번의 강제 경로 이동 사건이다.
- 출혈: 자발 이동 거리와 강제 이동 거리를 별도 사건으로 받아 추가 피해를 계산한다.
- 둔화: 자발 이동 속도에만 적용하고 밀치기 거리는 줄이지 않는다.

경로 시작은 0으로 고정한다. 강제 이동 또는 역행으로 음수 진행도가 되지 않는다.

### 장기 고정 탈출 모듈

`MovementRestrictionEscapeSystem`은 이동 제한을 `SpeedReduction`(둔화와 100% 미만 속도 배율), `HardControlStatus`(기절·지연·속박·에어본·빙결), `TemporalLockRuntime`(시간 정지)의 세 범주로 분리한다. 새 이동 제한 효과는 카드 이동 코드에 예외 분기를 추가하지 않고 이 분류 함수에 등록한다.

적이 활성 이동 제한을 가진 채 같은 `PathProgressMilli`에 300틱(10초) 머물면 다음 30틱(1초) 동안 세 범주의 이동 제한을 모두 무시하고 기본 경로 이동을 수행한다. 원래 상태를 해제하거나 지속시간을 되돌리지 않으며 화상·중독·출혈 틱도 정상 진행한다. 30틱이 끝났을 때 제한 상태가 남아 있으면 다시 적용되고, 실제 경로 진행도가 변한 시점부터 300틱 감시를 새로 시작한다. 두 시간값은 `run.movementEscapeStationaryTicks`, `run.movementEscapeImmunityTicks`에서 조정한다.

## 7. 생성, 분열, 복제 시 상속

생성된 추가 가지는 기본적으로 생성 카드 이전에 적용된 상태를 상속하지 않는다.

- 분열 원본 가지: 기존 상태 유지
- 분열 추가 가지: 기존 상태 미상속
- 분열 이후 카드가 적용한 상태: 모든 continuation 가지에 적용
- 복제: 카드 정의가 명시한 상태만 복사하며 `inherited = true`로 표시
- 과잉 복제: 허용 태그와 감쇠율에 맞는 상태/바인딩만 추가 상속
- 부활: 카드별 변환 정책을 적용하며, 보상 출처나 소비 토큰은 초기화하지 않음
- 거울/잔상: 원본 피해 전달용 링크만 가지며 임의 상태 복사 금지

상속된 상태도 원래 `SourceTowerId`, `SourceCardId`를 유지한다. 새 개체가 상태의 소유자라는 이유로 출처를 덮어쓰지 않는다.

## 8. Common·Uncommon 확장 상태 계약

아래 항목은 현재 Phase 1에서 활성화된 Common·Uncommon 상태의 최소 의미 계약이다. 수치, 주기, 반경과 발동 상한은 밸런스 JSON이 최종 원본이다.

| 상태 | 최소 규칙 |
| --- | --- |
| 출혈 | 이동/강제 이동 사건에서 추가 물리 피해 |
| 저주 | 다른 상태 피해와 지속시간 증가, 사망 전염 가능 |
| 빙결/냉기 | 냉기 최대 중첩에서 빙결, 해제 후 짧은 면역 |
| 속박 | 이동만 제한 |
| 에어본 | 피격 가능한 공중 정지와 착지 사건 |
| 봉인 | 보호막, 치유, 순간이동, 소환 같은 특수 능력 차단 |
| 부식 | 방어력과 최대 체력 감소, 최대 체력 하한 보장 |
| 공포 | 역주행 후 반동 가속 |
| 처형 표식 | 데이터 임계치 이하에서 사망 요청 |
| 과부하 저항 | 적 프로그램 재실행 뒤 이동 속도와 상태 저항 증가 |

## 9. 해제와 만료

- `Dispellable = false`인 시스템 상태, 가계 링크, 부활 토큰은 해제할 수 없다.
- 해제는 상태 InstanceId 정렬 순으로 처리한다.
- “하나 해제”는 정의된 우선순위가 없으면 가장 오래 남은 해제 가능 상태를 선택하고 동률이면 InstanceId가 작은 상태를 선택한다.
- 해제는 자연 만료와 다른 `StatusDispelled` 사건이다. “만료될 때” 트리거는 해제에 반응하지 않는 것이 기본값이다.
- Subject가 사망하면 `OnDeath` 바인딩 처리 후 일반 상태를 제거한다. 사망 전염은 제거 전에 읽은 불변 상태 목록을 사용한다.

표현 계층과 headless 테스트는 `EnemySnapshot.StatusDetails`에서 InstanceId, 타입, 원인 개체/타워/카드, 중첩, 강도, 남은 틱, 최대 중첩, 틱 간격, 방어 무시를 읽는다. 이 스냅샷 배열을 수정해도 내부 상태 인스턴스는 바뀌지 않는다.

### Stage01 상태 표현 계약

- 화상 중인 적은 원본 스프라이트 색을 보존한 채 주황색으로 틴트하고, 위로 튀는 불똥을 표시한다.
- 중독 중인 적은 초록색으로 틴트하고, 천천히 올라오는 거품을 표시한다.
- 두 상태가 동시에 있으면 중첩 비율에 따라 두 색을 혼합하고 두 파티클을 함께 재생한다.
- 파티클 상한은 적 한 개체당 화상 8개, 중독 6개다.
- 적 뷰가 풀로 반환될 때 틴트, 중첩 표시, 파티클을 모두 초기화한다.
- 이 연출은 `StatusSnapshot`의 현재 상태만 반영하며 상태 적용, 피해, 만료 판정에 관여하지 않는다.

## 10. 필수 테스트

- 화상과 중독이 적용 틱이 아닌 예약 틱부터 정확한 횟수로 피해를 준다.
- 같은 출처는 병합되고 다른 타워 출처는 분리된다.
- 둔화가 여러 개여도 60%를 넘지 않는다.
- 표식 타깃 우선과 피해 상한이 결정적이다.
- 천공이 방어력과 탄환 관통에 모두 반영된다.
- 일반 기절 중 DOT가 계속되며 보스 기절은 저항 게이지로 변환된다.
- `[화상, 분열]`의 추가 가지가 이전 화상을 상속하지 않는다.
- `[분열, 화상]`의 두 가지에는 화상이 적용된다.
- 화상/중독/동시 상태의 색과 파티클이 맞고 풀 재사용 시 완전히 초기화된다.
